# v0.10.0 最近活动时间线 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有“最近资源 ID 列表”升级为可筛选、分页、按资源折叠并可展开明细的最近活动时间线。

**Architecture:** SQLite 保留每次动作明细，Rust repository 负责 30 天/1,000 条清理、按资源聚合、筛选和分页；React 在“最近”工作区使用专用时间线，其他工作区继续使用现有虚拟资源网格。文件动作成功与活动写入解耦，活动失败不得反向报告文件动作失败。

**Tech Stack:** Tauri 2, React 18, TypeScript, Vite, Rust, SQLite/sqlx, chrono, Vitest, Testing Library.

---

## Source Context

- 规格：`I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md`
- 当前活动 migration：`I:\GameResManger\src-tauri\migrations\0005_recent_asset_actions.sql`
- 当前实现把最近活动压缩成 `recentAssetIds`，再从全量 `assets` 中拼装资源网格。
- v0.10.0 接受 `preview_media` 类型契约，但本版本不产生媒体预览记录。

## Safety Rules

- 不删除、移动、重命名或修改原始素材文件。
- 历史清理只删除 SQLite `recent_asset_actions` 行。
- 不提供手动清空历史。
- 不执行任何批量文件删除命令。

## File Structure

- Create: `I:\GameResManger\src-tauri\migrations\0009_recent_activity_timeline.sql`
  - 重建活动表 CHECK 约束，允许 `preview_media`，补聚合查询索引。
- Modify: `I:\GameResManger\src-tauri\src\models.rs`
  - 定义活动请求、明细、聚合项和分页响应。
- Modify: `I:\GameResManger\src-tauri\src\db.rs`
  - 活动验证、事务写入、清理、时间边界和分页聚合查询。
- Modify: `I:\GameResManger\src-tauri\src\commands.rs`
  - 暴露活动分页 command。
- Modify: `I:\GameResManger\src-tauri\src\lib.rs`
  - 注册 command，并在启动后非阻塞清理活动。
- Modify: `I:\GameResManger\src\types\asset.ts`
  - 同步活动领域类型。
- Modify: `I:\GameResManger\src\api\tauri.ts`
  - 新增 `listRecentActivity`，移除旧列表 API 使用。
- Create: `I:\GameResManger\src\components\RecentActivityTimeline.tsx`
  - 时间线卡、展开明细、选择、双击打开、加载更多。
- Create: `I:\GameResManger\src\components\RecentActivityTimeline.test.tsx`
  - 时间线组件行为测试。
- Modify: `I:\GameResManger\src\components\LibrarySidebar.tsx`
  - 最近上下文的时间和动作筛选。
- Modify: `I:\GameResManger\src\components\LibrarySidebar.test.tsx`
  - 最近筛选测试。
- Modify: `I:\GameResManger\src\App.tsx`
  - 独立活动状态、请求、分页和工作区切换。
- Modify: `I:\GameResManger\src\components\EmptyState.tsx`
  - 最近活动空状态。
- Modify: `I:\GameResManger\src\styles.css`
  - 时间线布局。
- Modify: `I:\GameResManger\tests\smoke\README.md`
- Modify: release metadata files to `0.10.0`.

## Task Status Board

| Done | Task | Scope | Required Verification |
|------|------|-------|-----------------------|
| [x] | Task 97 | 活动 migration、类型和清理规则 | `cargo test recent_activity_tests`; `cargo check` |
| [x] | Task 98 | 聚合筛选与分页 repository | `cargo test recent_activity_tests`; `cargo check` |
| [x] | Task 99 | Tauri command 与 TypeScript 契约 | `cargo check`; `npm run build` |
| [x] | Task 100 | 最近筛选侧栏与时间线组件 | focused frontend tests; `npm run build` |
| [x] | Task 101 | App 状态串联和旧最近网格移除 | `npm test`; `npm run build` |
| [~] | Task 102 | 烟测、版本同步与发布验证 | full frontend/backend verification; `npm run tauri dev` |

**Task 97 Notes:** migration 0009 已重建 CHECK 约束并补聚合索引；写入与清理在同一事务内完成，测试通过，仅因 Task 99 尚未调用 cleanup 函数产生临时 dead_code 警告。

**Task 98 Notes:** 已定义 RecentActivityRequest/Item/Response，实现按资源聚合的分页查询；today 使用传入时区当天零点，week 为滚动 7 天；计数与明细分离（明细每资源最多 50 条）；所有动态条件通过 QueryBuilder 绑定；查询在单一 SQLite 事务内完成；latest_action_type 通过窗口函数在过滤后的结果中计算，复用动作筛选并消除 N+1；新增 action-filter 回归测试、第二页稳定性测试和 migration 0009 CHECK 约束测试。测试通过。DST 切换日零点偏移需要 IANA 时区数据库（chrono-tz）才能完全精确，当前按固定偏移近似，与规格中显式传入固定偏移的测试策略一致。未使用的模型/函数等待 Task 99 接入 command。

**Task 99 Notes:** record_recent_asset_action command 已委托 repository 验证并移除 command 层白名单；新增并注册 list_recent_activity command；应用启动后非阻塞执行 cleanup_recent_asset_actions；TypeScript 新增 RecentActionType/RecentActivityPeriod/Request/Item/Response 与 listRecentActivity wrapper；移除 api/tauri.ts 中的 listRecentAssetActions 及 App.tsx 中对其的依赖（保留 recentAssetIds 状态给 Task 101 清理）。`cargo check` 与 `npm run build` 通过；`list_recent_asset_actions` repository 函数仍保留供 db.rs 测试使用，lib 构建报 dead_code 警告。

**Task 100 Notes:** 新建 `RecentActivityTimeline` 组件与测试，实现单资源折叠卡片、选择、展开/收起明细、双击打开（缺失资源禁用）、加载更多；`LibrarySidebar` 的 "recent" 上下文替换为时间和动作类型筛选，不再调用 `onFilterChange("recent")`；`EmptyState` 新增 `no-recent-activity` 与 `no-recent-filter-results` 空状态；补全时间线样式。为保持与现有 App.tsx/测试的兼容性，新筛选 props 设为可选并带默认值，Task 101 接入真实状态时可替换为必传。`npm test`（137 项）与 `npm run build` 均通过。

**Task 101 Notes:** 移除 `recentAssetIds` 状态与旧 `listRecentAssetActions` 加载逻辑；新增 `recentPeriod`/`recentActionType`/`recentItems`/`recentTotalCount`/`recentLoading`/`recentLoadingMore` 状态；`activeSection === "recent"` 时通过 `listRecentActivity` 拉取并追加分页；筛选变化清空选择并重新查询；最近工作区渲染 `RecentActivityTimeline`，隐藏 `SearchToolbar`/`FilterPanel`/`ActiveFilterChips`/资源网格 footer；文件打开、定位、复制成功后使用非阻塞 `.then().catch()` 记录活动并刷新可见时间线，活动记录失败不影响已成功的文件动作；`LibrarySidebar` 的筛选 props 改为必传并修复所有测试 fixtures。`npm test`（137 项）与 `npm run build` 均通过。

**Task 102 Notes:** 版本号同步至 `0.10.0`（package.json、package-lock.json、Cargo.toml、Cargo.lock、tauri.conf.json）；`tauri.conf.json` 的 `dataDirectory` 更新为 `webview-v0.10.0`；README 更新版本、特性描述与验证基线；tests/smoke/README.md 新增 v0.10.0 最近活动时间线烟测清单与发布验证记录；新建 docs/releases/v0.10.0.md。元数据测试（versionConsistency、viteCacheBust、tauriWebviewCache）3 项通过；前端测试 138 项通过（含新增 App 最近活动竞态回归测试）；`npm run build` 成功并输出 `v0_10_0` 版本化资源；Rust 测试 147 项 + build script 1 项通过；`cargo check` 通过并保留一处预期的 `list_recent_asset_actions` dead_code 警告。`npm run tauri dev` 在 headless 环境中编译并启动 `game-resource-manager.exe` 成功，但无显示服务器导致无法渲染窗口，交互式 GUI 烟测需在带显示的工作站手动完成；审查后补充修复：App.tsx 为最近活动请求增加序号丢弃、筛选变化立即清空旧列表、最近工作区隐藏扫描提示与 ScanStatusBar，复审后又补充按请求 ID 绑定 initial/loadMore 加载态以避免被淘汰请求遗留 loading 状态。未创建 Git 提交或发布包。

---

### Task 97: Add Activity Schema, Types, And Retention

**Files:**
- Create: `I:\GameResManger\src-tauri\migrations\0009_recent_activity_timeline.sql`
- Modify: `I:\GameResManger\src-tauri\src\models.rs`
- Modify: `I:\GameResManger\src-tauri\src\db.rs`

- [ ] **Step 1: Write failing retention and validation tests**

Add `recent_activity_tests` in `db.rs` with a one-connection in-memory pool and foreign keys enabled. Reuse the production `assets` column list.

```rust
#[tokio::test]
async fn accepts_all_supported_activity_types() {
    let db = setup_db().await;
    let asset_id = insert_asset(&db, "sound.wav", false).await;
    for action in ["open_file", "reveal_folder", "copy_path", "preview_media"] {
        record_recent_asset_action_at(&db, asset_id, action, "2026-06-13T10:00:00Z")
            .await
            .unwrap();
    }
    let count: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM recent_asset_actions")
        .fetch_one(&db).await.unwrap();
    assert_eq!(count, 4);
}

#[tokio::test]
async fn rejects_unknown_activity_type() {
    let db = setup_db().await;
    let asset_id = insert_asset(&db, "sound.wav", false).await;
    let error = record_recent_asset_action_at(
        &db, asset_id, "selected_asset", "2026-06-13T10:00:00Z"
    ).await.unwrap_err();
    assert!(error.to_string().contains("unsupported recent action type"));
}

#[tokio::test]
async fn cleanup_removes_old_rows_and_keeps_latest_thousand() {
    let db = setup_db().await;
    let asset_id = insert_asset(&db, "sound.wav", false).await;
    insert_activity_series(&db, asset_id, 1005, "2026-06-01T00:00:00Z").await;
    record_recent_asset_action_at(
        &db, asset_id, "open_file", "2026-04-01T00:00:00Z"
    ).await.unwrap();

    cleanup_recent_asset_actions_at(&db, "2026-06-13T00:00:00Z")
        .await.unwrap();

    let count: i64 = sqlx::query_scalar("SELECT COUNT(*) FROM recent_asset_actions")
        .fetch_one(&db).await.unwrap();
    assert_eq!(count, 1000);
    let old: i64 = sqlx::query_scalar(
        "SELECT COUNT(*) FROM recent_asset_actions WHERE created_at < '2026-05-14T00:00:00Z'"
    ).fetch_one(&db).await.unwrap();
    assert_eq!(old, 0);
}
```

- [ ] **Step 2: Run tests and verify failure**

Run:

```powershell
cd I:\GameResManger\src-tauri
cargo test recent_activity_tests
```

Expected: FAIL because the timestamp-injectable write and cleanup functions do not exist.

- [ ] **Step 3: Add migration 0009**

Create:

```sql
PRAGMA foreign_keys = OFF;

CREATE TABLE recent_asset_actions_new (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  asset_id INTEGER NOT NULL,
  action_type TEXT NOT NULL CHECK (
    action_type IN ('open_file', 'reveal_folder', 'copy_path', 'preview_media')
  ),
  created_at TEXT NOT NULL,
  FOREIGN KEY (asset_id) REFERENCES assets(id) ON DELETE CASCADE
);

INSERT INTO recent_asset_actions_new (id, asset_id, action_type, created_at)
SELECT id, asset_id, action_type, created_at FROM recent_asset_actions;

DROP TABLE recent_asset_actions;
ALTER TABLE recent_asset_actions_new RENAME TO recent_asset_actions;

CREATE INDEX idx_recent_asset_actions_created_at
ON recent_asset_actions(created_at DESC, id DESC);

CREATE INDEX idx_recent_asset_actions_asset_created
ON recent_asset_actions(asset_id, created_at DESC, id DESC);

CREATE INDEX idx_recent_asset_actions_type_created
ON recent_asset_actions(action_type, created_at DESC, id DESC);

PRAGMA foreign_keys = ON;
```

- [ ] **Step 4: Add typed action validation and injectable write**

In `models.rs` add:

```rust
pub const RECENT_ACTION_TYPES: [&str; 4] = [
    "open_file", "reveal_folder", "copy_path", "preview_media",
];

pub fn validate_recent_action_type(value: &str) -> anyhow::Result<()> {
    anyhow::ensure!(
        RECENT_ACTION_TYPES.contains(&value),
        "unsupported recent action type"
    );
    Ok(())
}
```

In `db.rs`:

```rust
pub async fn record_recent_asset_action_at(
    db: &Db,
    asset_id: i64,
    action_type: &str,
    created_at: &str,
) -> anyhow::Result<()> {
    crate::models::validate_recent_action_type(action_type)?;
    let mut tx = db.begin().await?;
    sqlx::query(
        "INSERT INTO recent_asset_actions (asset_id, action_type, created_at)
         VALUES (?1, ?2, ?3)"
    )
    .bind(asset_id).bind(action_type).bind(created_at)
    .execute(&mut *tx).await?;
    cleanup_recent_asset_actions_in_tx(&mut tx, created_at).await?;
    tx.commit().await?;
    Ok(())
}

pub async fn record_recent_asset_action(
    db: &Db, asset_id: i64, action_type: &str
) -> anyhow::Result<()> {
    record_recent_asset_action_at(
        db, asset_id, action_type, &Utc::now().to_rfc3339()
    ).await
}
```

- [ ] **Step 5: Implement retention**

```rust
async fn cleanup_recent_asset_actions_in_tx(
    tx: &mut sqlx::Transaction<'_, sqlx::Sqlite>,
    now: &str,
) -> anyhow::Result<()> {
    let now = chrono::DateTime::parse_from_rfc3339(now)?.with_timezone(&Utc);
    let cutoff = (now - chrono::Duration::days(30)).to_rfc3339();
    sqlx::query("DELETE FROM recent_asset_actions WHERE created_at < ?1")
        .bind(cutoff).execute(&mut **tx).await?;
    sqlx::query(
        "DELETE FROM recent_asset_actions
         WHERE id NOT IN (
           SELECT id FROM recent_asset_actions
           ORDER BY created_at DESC, id DESC LIMIT 1000
         )"
    ).execute(&mut **tx).await?;
    Ok(())
}

pub async fn cleanup_recent_asset_actions_at(db: &Db, now: &str) -> anyhow::Result<()> {
    let mut tx = db.begin().await?;
    cleanup_recent_asset_actions_in_tx(&mut tx, now).await?;
    tx.commit().await?;
    Ok(())
}

pub async fn cleanup_recent_asset_actions(db: &Db) -> anyhow::Result<()> {
    cleanup_recent_asset_actions_at(db, &Utc::now().to_rfc3339()).await
}
```

- [ ] **Step 6: Verify and commit**

Run:

```powershell
cd I:\GameResManger\src-tauri
cargo test recent_activity_tests
cargo check
```

Expected: retention and validation tests pass.

```powershell
git add src-tauri/migrations/0009_recent_activity_timeline.sql src-tauri/src/models.rs src-tauri/src/db.rs
git commit -m "feat: add recent activity retention"
```

---

### Task 98: Implement Aggregated Activity Query

**Files:**
- Modify: `I:\GameResManger\src-tauri\src\models.rs`
- Modify: `I:\GameResManger\src-tauri\src\db.rs`

- [ ] **Step 1: Write failing aggregation tests**

```rust
#[tokio::test]
async fn groups_actions_by_asset_with_counts_and_details() {
    let db = setup_db().await;
    let sound = insert_asset(&db, "sound.wav", false).await;
    let video = insert_asset(&db, "hero.mp4", false).await;
    seed_action(&db, sound, "copy_path", "2026-06-13T09:00:00Z").await;
    seed_action(&db, sound, "open_file", "2026-06-13T10:00:00Z").await;
    seed_action(&db, video, "reveal_folder", "2026-06-12T10:00:00Z").await;

    let page = list_recent_activity_at(
        &db,
        RecentActivityRequest {
            period: "all".into(), action_type: "all".into(), limit: 20, offset: 0
        },
        "2026-06-13T12:00:00+08:00",
    ).await.unwrap();

    assert_eq!(page.total_count, 2);
    assert_eq!(page.items[0].asset.id, sound);
    assert_eq!(page.items[0].latest_action_type, "open_file");
    assert_eq!(page.items[0].action_count, 2);
    assert_eq!(page.items[0].copy_path_count, 1);
    assert_eq!(page.items[0].actions.len(), 2);
}

#[tokio::test]
async fn filters_today_week_and_action_type() {
    let db = setup_db().await;
    let asset = insert_asset(&db, "sound.wav", false).await;
    seed_action(&db, asset, "copy_path", "2026-06-13T01:00:00Z").await;
    seed_action(&db, asset, "open_file", "2026-06-07T01:00:00Z").await;

    let today = request_page(&db, "today", "copy_path", "2026-06-13T12:00:00Z").await;
    assert_eq!(today.items.len(), 1);
    assert_eq!(today.items[0].action_count, 1);

    let week = request_page(&db, "week", "all", "2026-06-13T12:00:00Z").await;
    assert_eq!(week.items[0].action_count, 2);
}

#[tokio::test]
async fn keeps_missing_assets_and_uses_stable_pagination() {
    let db = setup_db().await;
    let a = insert_asset(&db, "a.wav", true).await;
    let b = insert_asset(&db, "b.wav", false).await;
    seed_action(&db, a, "open_file", "2026-06-13T10:00:00Z").await;
    seed_action(&db, b, "copy_path", "2026-06-13T10:00:00Z").await;
    let page = request_page_with_limit(&db, 1, 0).await;
    assert_eq!(page.total_count, 2);
    assert_eq!(page.items.len(), 1);
    assert!(page.items[0].asset.id == a || page.items[0].asset.id == b);
}
```

- [ ] **Step 2: Define request and response models**

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RecentActivityRequest {
    pub period: String,
    pub action_type: String,
    pub limit: i64,
    pub offset: i64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RecentActivityItem {
    pub asset: Asset,
    pub latest_action_type: String,
    pub latest_action_at: String,
    pub action_count: i64,
    pub open_file_count: i64,
    pub reveal_folder_count: i64,
    pub copy_path_count: i64,
    pub preview_media_count: i64,
    pub actions: Vec<RecentAssetAction>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RecentActivityResponse {
    pub items: Vec<RecentActivityItem>,
    pub total_count: i64,
    pub limit: i64,
    pub offset: i64,
}
```

- [ ] **Step 3: Implement validated bounds**

Add a helper that returns `(lower_bound, action_filter, limit, offset)`.

```rust
fn recent_activity_bounds(
    request: &RecentActivityRequest,
    now: chrono::DateTime<chrono::FixedOffset>,
) -> anyhow::Result<(Option<String>, Option<String>, i64, i64)> {
    let lower = match request.period.as_str() {
        "all" => None,
        "today" => {
            let start = now.date_naive().and_hms_opt(0, 0, 0).unwrap()
                .and_local_timezone(*now.offset()).single()
                .ok_or_else(|| anyhow::anyhow!("invalid day boundary"))?;
            Some(start.with_timezone(&Utc).to_rfc3339())
        }
        "week" => Some(
            (now - chrono::Duration::days(7))
                .with_timezone(&Utc)
                .to_rfc3339()
        ),
        _ => anyhow::bail!("unsupported recent period"),
    };
    let action = match request.action_type.as_str() {
        "all" => None,
        value => {
            crate::models::validate_recent_action_type(value)?;
            Some(value.to_string())
        }
    };
    Ok((lower, action, request.limit.clamp(1, 100), request.offset.max(0)))
}
```

`list_recent_activity` passes `chrono::Local::now().fixed_offset()` in production.
Tests pass explicit fixed offsets, for example `2026-06-13T12:00:00+08:00`, so
the “today” boundary does not depend on the test machine time zone.

- [ ] **Step 4: Implement query in two bounded phases**

Use `QueryBuilder<Sqlite>`:

1. Filter actions in a CTE.
2. Group by `asset_id`.
3. Count distinct grouped assets.
4. Fetch one page of asset IDs and aggregate columns.
5. Fetch at most 50 details for each returned asset.
6. Load `Asset` rows using the existing exact column list.

The grouped projection must use:

```sql
MAX(created_at) AS latest_action_at,
COUNT(*) AS action_count,
SUM(action_type = 'open_file') AS open_file_count,
SUM(action_type = 'reveal_folder') AS reveal_folder_count,
SUM(action_type = 'copy_path') AS copy_path_count,
SUM(action_type = 'preview_media') AS preview_media_count
```

Resolve `latest_action_type` with:

```sql
SELECT action_type
FROM recent_asset_actions
WHERE asset_id = ?1
  AND created_at = ?2
ORDER BY id DESC
LIMIT 1
```

- [ ] **Step 5: Verify and commit**

Run:

```powershell
cd I:\GameResManger\src-tauri
cargo test recent_activity_tests
cargo check
```

Expected: aggregation, filtering, missing-state and pagination tests pass.

```powershell
git add src-tauri/src/models.rs src-tauri/src/db.rs
git commit -m "feat: query grouped recent activity"
```

---

### Task 99: Expose Typed Activity API

**Files:**
- Modify: `I:\GameResManger\src-tauri\src\commands.rs`
- Modify: `I:\GameResManger\src-tauri\src\lib.rs`
- Modify: `I:\GameResManger\src\types\asset.ts`
- Modify: `I:\GameResManger\src\api\tauri.ts`

- [ ] **Step 1: Move action validation behind the repository**

Replace command validation with:

```rust
#[tauri::command]
pub async fn record_recent_asset_action(
    db: State<'_, SqlitePool>,
    asset_id: i64,
    action_type: String,
) -> Result<(), CommandError> {
    db::record_recent_asset_action(&*db, asset_id, &action_type)
        .await.map_err(Into::into)
}
```

- [ ] **Step 2: Add pagination command**

```rust
#[tauri::command]
pub async fn list_recent_activity(
    db: State<'_, SqlitePool>,
    request: crate::models::RecentActivityRequest,
) -> Result<crate::models::RecentActivityResponse, CommandError> {
    db::list_recent_activity(&*db, request).await.map_err(Into::into)
}
```

Register `commands::list_recent_activity`. Remove registration and frontend use of `list_recent_asset_actions`; keep the repository function only if tests still require it, otherwise delete it.

- [ ] **Step 3: Start cleanup non-blockingly**

In `lib.rs` clone the pool and extend the existing startup cleanup task:

```rust
if let Err(e) = db::cleanup_recent_asset_actions(&cleanup_pool).await {
    eprintln!("Failed to cleanup recent activity: {}", e);
}
```

- [ ] **Step 4: Add exact TypeScript contracts**

```ts
export type RecentActionType =
  | "open_file"
  | "reveal_folder"
  | "copy_path"
  | "preview_media";

export type RecentActivityPeriod = "all" | "today" | "week";

export type RecentActivityRequest = {
  period: RecentActivityPeriod;
  action_type: "all" | RecentActionType;
  limit: number;
  offset: number;
};

export type RecentActivityItem = {
  asset: Asset;
  latest_action_type: RecentActionType;
  latest_action_at: string;
  action_count: number;
  open_file_count: number;
  reveal_folder_count: number;
  copy_path_count: number;
  preview_media_count: number;
  actions: RecentAssetAction[];
};
```

- [ ] **Step 5: Add wrapper**

```ts
export async function listRecentActivity(
  request: RecentActivityRequest
): Promise<RecentActivityResponse> {
  return invoke<RecentActivityResponse>("list_recent_activity", { request });
}
```

- [ ] **Step 6: Verify and commit**

```powershell
cd I:\GameResManger\src-tauri
cargo check
cd I:\GameResManger
npm run build
```

```powershell
git add src-tauri/src/commands.rs src-tauri/src/lib.rs src/types/asset.ts src/api/tauri.ts
git commit -m "feat: expose recent activity API"
```

---

### Task 100: Build Recent Filters And Timeline

**Files:**
- Create: `I:\GameResManger\src\components\RecentActivityTimeline.tsx`
- Create: `I:\GameResManger\src\components\RecentActivityTimeline.test.tsx`
- Modify: `I:\GameResManger\src\components\LibrarySidebar.tsx`
- Modify: `I:\GameResManger\src\components\LibrarySidebar.test.tsx`
- Modify: `I:\GameResManger\src\components\EmptyState.tsx`
- Modify: `I:\GameResManger\src\styles.css`

- [x] **Step 1: Write failing timeline tests**

```tsx
it("selects a resource without expanding details", async () => {
  const onSelect = vi.fn();
  render(<RecentActivityTimeline {...props} onSelectAsset={onSelect} />);
  await userEvent.click(screen.getByRole("button", { name: "选择 sound.wav" }));
  expect(onSelect).toHaveBeenCalledWith(item.asset.id);
  expect(screen.queryByText("复制路径 · 2026")).not.toBeInTheDocument();
});

it("expands activity details independently", async () => {
  render(<RecentActivityTimeline {...props} />);
  await userEvent.click(screen.getByRole("button", { name: "展开 sound.wav 的活动明细" }));
  expect(screen.getByText(/复制路径/)).toBeInTheDocument();
});

it("opens available assets on double click and blocks missing assets", async () => {
  const onOpen = vi.fn();
  const { rerender } = render(<RecentActivityTimeline {...props} onOpenAsset={onOpen} />);
  fireEvent.doubleClick(screen.getByRole("button", { name: "选择 sound.wav" }));
  expect(onOpen).toHaveBeenCalledWith(item.asset);
  rerender(<RecentActivityTimeline {...props} items={[missingItem]} onOpenAsset={onOpen} />);
  fireEvent.doubleClick(screen.getByRole("button", { name: "选择 missing.wav" }));
  expect(onOpen).toHaveBeenCalledTimes(1);
});

it("loads another page", async () => {
  const onLoadMore = vi.fn();
  render(<RecentActivityTimeline {...props} canLoadMore onLoadMore={onLoadMore} />);
  await userEvent.click(screen.getByRole("button", { name: "加载更多活动" }));
  expect(onLoadMore).toHaveBeenCalled();
});
```

- [x] **Step 2: Write failing sidebar filter tests**

Add required props:

```ts
recentPeriod: RecentActivityPeriod;
recentActionType: "all" | RecentActionType;
onRecentPeriodChange: (value: RecentActivityPeriod) => void;
onRecentActionTypeChange: (value: "all" | RecentActionType) => void;
```

Test:

```tsx
it("changes recent period and action filters", async () => {
  const onPeriod = vi.fn();
  const onAction = vi.fn();
  renderSidebar({
    activeSection: "recent",
    onRecentPeriodChange: onPeriod,
    onRecentActionTypeChange: onAction,
  });
  await userEvent.click(screen.getByRole("button", { name: "最近 7 天" }));
  await userEvent.click(screen.getByRole("button", { name: "复制路径" }));
  expect(onPeriod).toHaveBeenCalledWith("week");
  expect(onAction).toHaveBeenCalledWith("copy_path");
});
```

- [x] **Step 3: Implement timeline contract**

```ts
type Props = {
  items: RecentActivityItem[];
  selectedIds: number[];
  totalCount: number;
  loading: boolean;
  loadingMore: boolean;
  canLoadMore: boolean;
  onSelectAsset: (assetId: number) => void;
  onOpenAsset: (asset: Asset) => void;
  onLoadMore: () => void;
};
```

Use local `Set<number>` state for expanded asset IDs. Render summary counts only when greater than zero. Use explicit Chinese action labels from a constant map.

- [x] **Step 4: Implement recent sidebar filters**

Replace the old single “最近使用” filter button with two sections. Do not call `onFilterChange("recent")`; entering the navigation section already selects the activity workspace.

```tsx
{[
  ["all", "全部"],
  ["today", "今天"],
  ["week", "最近 7 天"],
].map(([value, label]) => (
  <button
    key={value}
    className={recentPeriod === value ? "nav-item active" : "nav-item"}
    onClick={() => onRecentPeriodChange(value as RecentActivityPeriod)}
  >
    {label}
  </button>
))}
```

- [x] **Step 5: Add empty state variants**

Extend `EmptyState`:

```ts
| "no-recent-activity"
| "no-recent-filter-results"
```

Use:

```text
暂无最近活动 / 打开、定位或复制资源后会显示在这里。
当前筛选没有活动 / 尝试切换时间范围或动作类型。
```

- [x] **Step 6: Verify and commit**

```powershell
npm test -- src/components/RecentActivityTimeline.test.tsx src/components/LibrarySidebar.test.tsx
npm run build
```

```powershell
git add src/components/RecentActivityTimeline.tsx src/components/RecentActivityTimeline.test.tsx src/components/LibrarySidebar.tsx src/components/LibrarySidebar.test.tsx src/components/EmptyState.tsx src/styles.css
git commit -m "feat: add recent activity timeline"
```

---

### Task 101: Wire Activity Workspace Into App

**Files:**
- Modify: `I:\GameResManger\src\App.tsx`
- Modify: affected frontend test fixtures

- [ ] **Step 1: Remove old recent ID state**

Delete:

```ts
const [recentAssetIds, setRecentAssetIds] = useState<number[]>([]);
```

Remove `listRecentAssetActions()` from `loadData`, remove `"recent"` from `activeFilter` special handling, and delete `displayAssets` recent-ID mapping.

- [ ] **Step 2: Add activity state**

```ts
const [recentPeriod, setRecentPeriod] =
  useState<RecentActivityPeriod>("all");
const [recentActionType, setRecentActionType] =
  useState<"all" | RecentActionType>("all");
const [recentItems, setRecentItems] = useState<RecentActivityItem[]>([]);
const [recentTotalCount, setRecentTotalCount] = useState(0);
const [recentLoading, setRecentLoading] = useState(false);
const [recentLoadingMore, setRecentLoadingMore] = useState(false);
```

- [ ] **Step 3: Add first-page and load-more callbacks**

```ts
const loadRecentActivity = useCallback(async () => {
  setRecentLoading(true);
  try {
    const page = await listRecentActivity({
      period: recentPeriod,
      action_type: recentActionType,
      limit: 40,
      offset: 0,
    });
    setRecentItems(page.items);
    setRecentTotalCount(page.total_count);
  } catch (error) {
    showError(error);
  } finally {
    setRecentLoading(false);
  }
}, [recentActionType, recentPeriod, showError]);
```

Load only when `activeSection === "recent"`. Filter changes clear selection and reset the list.

- [ ] **Step 4: Refresh activity after successful actions**

Keep file actions non-blocking:

```ts
const refreshRecentIfVisible = useCallback(() => {
  if (activeSection === "recent") void loadRecentActivity();
}, [activeSection, loadRecentActivity]);
```

After `recordRecentAssetAction`, update the time line through `refreshRecentIfVisible()`. Do not include the record call inside the `try` block that determines whether opening/revealing/copying succeeded.

- [ ] **Step 5: Render timeline only for recent section**

```tsx
{activeSection === "recent" ? (
  recentItems.length === 0 && !recentLoading ? (
    <EmptyState
      variant={
        recentPeriod === "all" && recentActionType === "all"
          ? "no-recent-activity"
          : "no-recent-filter-results"
      }
    />
  ) : (
    <RecentActivityTimeline
      items={recentItems}
      selectedIds={selectedIds}
      totalCount={recentTotalCount}
      loading={recentLoading}
      loadingMore={recentLoadingMore}
      canLoadMore={recentItems.length < recentTotalCount}
      onSelectAsset={(id) => setSelectedIds([id])}
      onOpenAsset={handleOpenFile}
      onLoadMore={handleLoadMoreRecent}
    />
  )
) : (
  /* existing EmptyState / AssetGrid branch */
)}
```

Hide `SearchToolbar`, `FilterPanel`, `ActiveFilterChips`, scan summary and grid result footer while in recent mode. The right `DetailsPanel` remains unchanged.

- [ ] **Step 6: Run full frontend regression and commit**

```powershell
npm test
npm run build
```

```powershell
git add src/App.tsx src/components
git commit -m "feat: integrate recent activity workspace"
```

---

### Task 102: Smoke QA, Version Sync, And Release Verification

**Files:**
- Modify: `I:\GameResManger\tests\smoke\README.md`
- Modify: `I:\GameResManger\README.md`
- Modify: `I:\GameResManger\package.json`
- Modify: `I:\GameResManger\package-lock.json`
- Modify: `I:\GameResManger\src-tauri\Cargo.toml`
- Modify: `I:\GameResManger\src-tauri\Cargo.lock`
- Modify: `I:\GameResManger\src-tauri\tauri.conf.json`
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md`

- [x] **Step 1: Add smoke checklist**

Append checks for:

- opening, revealing and copying three different resources;
- recent cards ordered by latest action;
- same resource folded into one card;
- expand/collapse details;
- today/week/action filters;
- missing resource history;
- double-click open;
- load more;
- source files unchanged.

- [x] **Step 2: Set release metadata**

Set `0.10.0` in package, Cargo and Tauri metadata. Set:

```json
"dataDirectory": "webview-v0.10.0"
```

- [x] **Step 3: Run metadata and full verification**

```powershell
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check
```

- [x] **Step 4: Run desktop smoke**

```powershell
cd I:\GameResManger
npm run tauri dev
```

Expected: recent timeline, filters, expansion, selection and details work; no source file changes.

Run in headless environment: compiled and launched successfully, but no display server was available to render the window. Interactive items remain for manual workstation verification.

- [x] **Step 5: Update task board and commit**

Mark Tasks 97-102 complete only after their required verification passes. Task board updated; no commit created per instruction.

```powershell
git add README.md tests/smoke/README.md package.json package-lock.json src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/tauri.conf.json docs/superpowers/plans/2026-06-13-v0.10.0-recent-activity.md
git commit -m "chore: release v0.10.0"
```

---

## Acceptance Criteria

- 最近工作区使用专用分页时间线。
- 同一资源按最新活动折叠，明细可展开。
- 支持全部、今天、最近 7 天和动作筛选。
- 历史保留最近 30 天且最多 1,000 条。
- 缺失资源活动可见。
- 活动写入失败不反向报告文件动作失败。
- 其他工作区仍使用现有虚拟网格。
- 自动化检查与桌面烟测通过。

## Self-Review

- Spec coverage: 数据、清理、聚合、筛选、分页、折叠、缺失状态和安全边界均有任务。
- Placeholder scan: 无占位指令。
- Type consistency: `RecentActivityRequest/Item/Response` 在 Rust、TypeScript 和 API 中同名同字段。
- Scope check: 本计划不实现媒体播放，只预留 `preview_media` 类型。
