# v0.8.0 Collection Management Agent Prompts

用途：把 `2026-06-11-v0.8-collection-management.md` 转换成可直接交给其他 Agent 执行的提示词。

## 通用约束

- 默认工作目录：`I:\GameResManger`。
- 默认使用中文回复。
- 开始前必须读取：
  - `I:\GameResManger\AGENTS.md`
  - `I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md`
  - `G:\oklai999的策划仓库\游戏资源管理器\需求整理_v0.1.md`
- 开始和结束时运行 `git status --short`。
- 当前工作区已有计划文档改动。不得回滚、覆盖、清理或顺手格式化无关文件。
- 不删除、移动、重命名或修改原始素材文件。
- 删除集合只能删除 SQLite 中的 `collections` 和 `collection_assets` 整理数据。
- 不向素材目录写入数据库、索引、缩略图、缓存、元数据或生成文件。
- 禁止批量删除命令：`del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
- 不执行 `git reset --hard`、`git checkout --`。
- 不添加 AI 打标、文件删除、批量移动、批量重命名、图集、云同步或完整运行时预览。
- 使用 TDD：先添加或确认失败测试，再实现最小代码，再运行验证。
- 不创建 Git 提交，除非用户明确要求。
- 只执行被分配的 Task，不提前执行后续 Task。
- 完成任务后更新主计划中对应任务的状态和 Notes；若无法修改计划，回复中给出建议写入的状态备注。

---

## 总控 Prompt：连续执行 v0.8.0

```text
你是本项目的执行 Agent。请完整执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md

目标：
完成 v0.8.0 集合管理闭环，包括集合成员数量、重命名、描述编辑、批量加入、批量移出、安全删除，以及前端状态刷新和版本发布。

执行方式：
1. 使用 superpowers:executing-plans 或 superpowers:subagent-driven-development。
2. 严格按 Task 78 到 Task 83 顺序执行。
3. 每完成一个 Task，立即运行该任务要求的验证并更新任务板。
4. 测试失败时先定位根因，不跳过测试，不削弱断言。
5. Task 83 之前不得把版本号改为 0.8.0。
6. 不创建 Git 提交，除非用户明确要求。

关键产品语义：
- 集合名和描述保存前 trim；空名称必须拒绝。
- 集合名继续使用数据库唯一约束。
- 集合列表按名称排序并显示成员数量。
- 重复加入、重复移出必须幂等。
- “从当前集合移出”只在集合筛选已激活时显示。
- 删除当前集合后清空集合筛选和资源选择。
- 删除集合只删除应用内集合和成员关系，不删除资产索引或源文件。

最终必须运行：
- npm test
- npm run build
- cd I:\GameResManger\src-tauri
- cargo test
- cargo check

GUI 可用时再运行：
- cd I:\GameResManger
- npm run tauri dev

完成后回复：
- Task 78-83 的完成状态。
- 新增和修改文件清单。
- 集合 CRUD、数量和成员关系的实现摘要。
- 删除集合不会影响资产和源文件的验证证据。
- 前端测试、构建、Rust 测试和 cargo check 的实际结果。
- GUI 烟测是否执行；未执行需说明原因。
- 当前 git status 摘要。
```

---

## Prompt 1：Task 78 - 集合 Repository 生命周期

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 78。

目标：
在 Rust repository 层补齐集合成员数量、更新、批量移出和安全删除。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\src-tauri\src\models.rs
- I:\GameResManger\src-tauri\src\db.rs
- I:\GameResManger\src-tauri\migrations\0001_initial.sql

只允许修改：
- I:\GameResManger\src-tauri\src\models.rs
- I:\GameResManger\src-tauri\src\db.rs
- 主计划 Task 78 的状态和备注

执行要求：
1. 运行 git status --short，保留所有无关改动。
2. 按主计划先添加以下失败测试：
   - list_collections_includes_asset_count
   - update_collection_trims_name_and_description
   - update_collection_rejects_blank_name
   - remove_assets_from_collection_is_idempotent
   - delete_collection_removes_links_but_not_assets
3. Collection 模型增加 asset_count: i64。
4. list_collections 使用 LEFT JOIN + COUNT 返回数量，并按名称稳定排序。
5. create_collection 返回 asset_count = 0。
6. 实现 update_collection、remove_assets_from_collection、delete_collection。
7. 现有 remove_asset_from_collection 委托给批量移出函数。
8. 删除测试必须证明 assets 记录仍存在，只删除集合和关联。
9. 不修改 commands、前端或版本号。

必须运行：
cd I:\GameResManger\src-tauri
cargo test db::collections_tests
cargo check

完成后回复：
- 修改文件。
- 新增测试及结果。
- SQL 查询和事务策略摘要。
- 删除安全性证据。
- 建议写入 Task 78 Notes 的一句话。
- git status 摘要。
```

---

## Prompt 2：Task 79 - Tauri Commands 与前端类型契约

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 79。

前置条件：
- Task 78 已完成。
- Collection Rust 模型已有 asset_count。
- db.rs 已有 update_collection、remove_assets_from_collection、delete_collection。
- 如果前置条件缺失，停止并报告，不要自行重做 Task 78。

目标：
暴露集合更新、批量移出和删除命令，并同步 TypeScript Collection 与 API wrapper。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\src-tauri\src\commands.rs
- I:\GameResManger\src-tauri\src\lib.rs
- I:\GameResManger\src\types\asset.ts
- I:\GameResManger\src\api\tauri.ts

只允许修改：
- I:\GameResManger\src-tauri\src\commands.rs
- I:\GameResManger\src-tauri\src\lib.rs
- I:\GameResManger\src\types\asset.ts
- I:\GameResManger\src\api\tauri.ts
- 因 Collection 类型新增 asset_count 而必须修复的测试 factory
- 主计划 Task 79 的状态和备注

执行要求：
1. 新增并注册命令：
   - update_collection
   - remove_assets_from_collection
   - delete_collection
2. TypeScript Collection 增加 asset_count: number。
3. 所有测试 Collection factory 增加 asset_count: 0。
4. 新增类型化 wrapper：
   - updateCollection
   - removeAssetsFromCollection
   - deleteCollection
5. Tauri 前端参数使用 camelCase，Rust 命令参数保持 snake_case 映射惯例。
6. 不实现 UI，不改 App，不改版本号。

必须运行：
cd I:\GameResManger\src-tauri
cargo check
cd I:\GameResManger
npm run build

完成后回复：
- 命令和 wrapper 清单。
- Rust/TypeScript Collection 字段一致性。
- cargo check 和 npm build 结果。
- 建议写入 Task 79 Notes 的一句话。
- git status 摘要。
```

---

## Prompt 3：Task 80 - 集合管理界面

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 80。

前置条件：
- Task 79 已完成。
- Collection 类型已有 asset_count。
- 如果前置条件缺失，停止并报告。

目标：
创建集合管理器，在左侧显示集合成员数量，并支持编辑名称、描述和二次确认删除。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\src\components\LibrarySidebar.tsx
- I:\GameResManger\src\components\LibrarySidebar.test.tsx
- I:\GameResManger\src\styles.css

允许创建：
- I:\GameResManger\src\components\CollectionManager.tsx
- I:\GameResManger\src\components\CollectionManager.test.tsx

只允许修改：
- I:\GameResManger\src\components\LibrarySidebar.tsx
- I:\GameResManger\src\components\LibrarySidebar.test.tsx
- I:\GameResManger\src\styles.css
- 主计划 Task 80 的状态和备注

执行要求：
1. 先按主计划添加 CollectionManager 和 LibrarySidebar 失败测试。
2. CollectionManager 必须提供：
   - 集合列表和成员数量。
   - 集合名称输入。
   - 集合描述输入。
   - 保存集合按钮。
   - 删除集合按钮。
   - 明确的二次确认区域。
3. 删除确认文案必须包含“不会删除任何原始素材文件”。
4. 空名称禁用保存或不触发 onUpdate。
5. LibrarySidebar 集合标题增加“管理”入口。
6. 集合行显示 asset_count。
7. UI 文案使用中文，保持工具型和紧凑布局。
8. 不调用 Tauri API，不修改 App，不改版本号。

必须运行：
npm test -- src/components/CollectionManager.test.tsx src/components/LibrarySidebar.test.tsx
npm run build

完成后回复：
- 新增组件与 props。
- 测试覆盖行为。
- 安全删除文案。
- 测试和构建结果。
- 建议写入 Task 80 Notes 的一句话。
- git status 摘要。
```

---

## Prompt 4：Task 81 - 当前集合内移出操作

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 81。

前置条件：
- Task 79 已完成。
- DetailsPanel 当前仍支持单选详情和多选批量整理。

目标：
在当前集合筛选上下文中，为单选和多选资源提供“从当前集合移出”操作。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\src\components\DetailsPanel.tsx
- I:\GameResManger\src\components\DetailsPanel.test.tsx
- I:\GameResManger\src\styles.css

只允许修改：
- I:\GameResManger\src\components\DetailsPanel.tsx
- I:\GameResManger\src\components\DetailsPanel.test.tsx
- I:\GameResManger\src\styles.css
- 主计划 Task 81 的状态和备注

执行要求：
1. 先添加主计划指定的三类失败测试：
   - 单资源从当前集合移出。
   - 多资源批量移出。
   - 非集合筛选时不显示移出按钮。
2. 新增 props：
   - activeCollectionId: number | null
   - onRemoveFromCollection(collectionId, assetIds)
3. 单选按钮文案：从当前集合移出。
4. 多选按钮文案：从当前集合移出 N 个资源。
5. activeCollectionId 为 null 时不得显示移出操作。
6. 只发出回调，不直接调用 API。
7. 不修改 App，不改版本号。

必须运行：
npm test -- src/components/DetailsPanel.test.tsx
npm run build

完成后回复：
- Props 变化。
- 单选、多选和隐藏条件。
- 测试和构建结果。
- 建议写入 Task 81 Notes 的一句话。
- git status 摘要。
```

---

## Prompt 5：Task 82 - App 状态串联

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 82。

前置条件：
- Tasks 78-81 已完成。
- 新 API wrapper、CollectionManager props 和 DetailsPanel 移出 props 均已存在。
- 如果任何前置条件缺失，停止并列出缺失项。

目标：
在 App.tsx 中串联集合更新、删除、加入后数量刷新、当前集合移出和筛选状态清理。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\src\App.tsx
- I:\GameResManger\src\components\LibrarySidebar.tsx
- I:\GameResManger\src\components\DetailsPanel.tsx

只允许修改：
- I:\GameResManger\src\App.tsx
- 因新增必需 props 而需要修复的组件测试 fixtures
- 主计划 Task 82 的状态和备注

执行要求：
1. 导入 deleteCollection、removeAssetsFromCollection、updateCollection。
2. 加入集合成功后重新 listCollections，刷新 asset_count。
3. 实现 handleUpdateCollection，更新并按名称排序本地集合状态。
4. 实现 handleDeleteCollection：
   - 删除失败或不存在时显示错误。
   - 删除当前集合时清空 selectedCollectionId 和 selectedIds。
   - 成功提示明确原始素材未改动。
5. 实现 handleRemoveFromCollection：
   - 调用批量移出 API。
   - 刷新集合数量。
   - 清空当前选择。
   - 重新执行搜索，使已移出的卡片从当前集合结果消失。
6. 把新 handlers 传给 LibrarySidebar 和 DetailsPanel。
7. 更新所有必需测试 fixtures，但不改变无关测试语义。
8. 不改版本号。

必须运行：
npm test
npm run build

完成后回复：
- App handlers 和状态变化。
- 当前集合删除后的行为。
- 加入/移出后的数量与搜索刷新策略。
- 全部前端测试和构建结果。
- 建议写入 Task 82 Notes 的一句话。
- git status 摘要。
```

---

## Prompt 6：Task 83 - 烟测、版本同步与最终验证

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
中的 Task 83。

前置条件：
- Tasks 78-82 在主计划任务板中均为完成。
- 集合生命周期、管理 UI 和 App 串联已经实现。
- 如果任一前置任务未完成，停止并列出缺失项，不要提前改版本号。

目标：
补充 v0.8.0 集合管理烟测，统一发布版本，并运行完整自动化验证。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-11-v0.8-collection-management.md
- I:\GameResManger\README.md
- I:\GameResManger\tests\smoke\README.md
- I:\GameResManger\tests\versionConsistency.test.ts
- I:\GameResManger\tests\viteCacheBust.test.ts
- I:\GameResManger\tests\tauriWebviewCache.test.ts

只允许修改：
- I:\GameResManger\tests\smoke\README.md
- I:\GameResManger\README.md
- I:\GameResManger\package.json
- I:\GameResManger\package-lock.json
- I:\GameResManger\src-tauri\Cargo.toml
- I:\GameResManger\src-tauri\Cargo.lock
- I:\GameResManger\src-tauri\tauri.conf.json
- 主计划 Task 83 的状态、备注和 Progress Status
- 策划仓库中的总实施计划和里程碑状态（仅在明确可写时）

执行要求：
1. 添加主计划指定的 v0.8.0 Collection Management Smoke Test。
2. README 当前版本改为 0.8.0，并更新集合能力说明。
3. 同步以下版本为 0.8.0：
   - package.json
   - package-lock.json 根包
   - src-tauri/Cargo.toml
   - src-tauri/Cargo.lock 根包
   - src-tauri/tauri.conf.json
4. dataDirectory 改为 webview-v0.8.0。
5. 先运行版本一致性测试，再运行完整前后端验证。
6. GUI 可用时运行 npm run tauri dev；不可用时明确记录未执行。
7. 最后运行 git status --short，确认没有无关业务改动。
8. 所有验证通过后把主计划 Task 83 标为完成，并把 Progress Status 更新为 Released/Verified。
9. 不创建 Git 提交，除非用户明确要求。

必须运行：
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

GUI 可用时：
cd I:\GameResManger
npm run tauri dev

完成后回复：
- smoke 文档新增内容。
- 所有版本文件的最终值。
- 前端测试数量和结果。
- Vite 构建结果。
- Rust 测试数量和结果。
- cargo check 结果。
- GUI 烟测结果或未执行原因。
- 源文件安全边界复核。
- git status 摘要。
```

---

## 推荐派发顺序

1. Prompt 1：Task 78
2. Prompt 2：Task 79
3. Prompt 3：Task 80
4. Prompt 4：Task 81
5. Prompt 5：Task 82
6. Prompt 6：Task 83

Task 80 和 Task 81 在 Task 79 完成后可以并行执行；Task 82 必须等待两者都完成。
