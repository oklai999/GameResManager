# v0.10.1 Release Smoke And Packaging Agent Prompts

配套实施计划：

`I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`

配套上一阶段计划：

`I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md`

## 使用方式

- 这是 v0.10.1 的发布收口阶段，不是新功能阶段。
- 默认不改版本号，不创建 Git 提交，不打标签，不上传发布。
- Task 107 需要真实 Windows 桌面显示和媒体 fixture；没有显示环境时必须停止并报告。
- Task 108-110 可以由另一个 agent 接力，但必须基于 Task 107 的真实结果写文档。

---

## 总控 Prompt：连续执行 v0.10.1 发布收口

```text
你是“游戏资源管理器”项目的执行 Agent。请完整执行：
I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\README.md
3. I:\GameResManger\tests\smoke\README.md
4. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md
5. I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

执行方式：
1. 使用 superpowers:executing-plans 或 superpowers:subagent-driven-development。
2. 严格按 Task 107 -> 110 顺序执行。
3. 不要实现新功能。
4. 不要改版本号。
5. 除非用户明确要求，不要创建 Git 提交、tag 或 PR。
6. 如果没有 Windows GUI 显示环境，Task 107 不得标记完成；记录阻塞原因后停止或交回。

硬性安全边界：
- 不删除、移动、重命名或修改任何源素材文件。
- 不向源素材目录写入缓存、数据库、日志、缩略图或媒体文件。
- 不使用 del /s、rd /s、rmdir /s、Remove-Item -Recurse、rm -rf。
- 不扩大 assetProtocol.scope。
- 不复制、转码、生成波形或处理源媒体。

最终必须运行：
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check
cd I:\GameResManger
npx tauri build
.\package.ps1

必须产出：
- tests\smoke\README.md 中 v0.10.1 媒体矩阵的真实结果。
- docs\releases\v0.10.1.md，包含验证、GUI smoke、artifact 路径、大小、SHA-256、安全结论。
- README.md 链接到 docs/releases/v0.10.1.md。
- 主计划 Task 107-110 状态和 Notes。

完成后汇报：
- GUI smoke 结果。
- 自动化验证结果。
- 打包产物目录、文件大小、SHA-256。
- assetProtocol.scope 和 asset-media 安全复核。
- 源素材安全结论。
- git status 摘要。
```

---

## Task 107 Prompt：真实 Windows GUI 媒体烟测

```text
你正在执行“游戏资源管理器”v0.10.1 发布收口的 Task 107。

只执行 Task 107，不要执行打包和发布文档。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\tests\smoke\README.md 的 v0.10.1 Native Media Preview Smoke Test
3. I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md 中 Task 107
4. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md 的 Task 103-106 Notes

前置条件：
- 当前机器必须有可交互的 Windows 桌面显示。
- 必须有或能够选择一个仓库外 fixture 文件夹。
- fixture 至少包含 sample.mp3、sample.wav、sample.ogg、sample.mp4、sample.webm。
- 如果没有 incompatible.mp4 或 incompatible.webm，必须记录“未测”，不得标记通过。

允许修改：
- I:\GameResManger\tests\smoke\README.md
- I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

禁止：
- 不要删除、移动、重命名或修改 fixture 源文件。
- 不要把缓存、数据库、缩略图或日志写入 fixture 文件夹。
- 不要运行任何批量删除命令。
- 不要改代码、版本号或 release 文档。

执行步骤：
1. 运行 git status --short，记录起始状态。
2. 运行 npm run tauri dev。
3. 在应用中添加 fixture 文件夹并扫描。
4. 对 MP3、WAV、OGG、MP4、WebM 逐个验证 metadata、play、pause、seek、volume、切换停止、preview_media 记录、文件动作、源文件不变。
5. 对 incompatible.mp4 / incompatible.webm 验证失败回退和文件动作保留；没有样本则记录未测。
6. 更新 tests\smoke\README.md 中 v0.10.1 媒体矩阵，必须区分通过、失败、未测。
7. 更新主计划 Task 107 Notes，记录 fixture 路径、通过格式、失败格式、未测格式、源文件安全结论。

完成后汇报：
- 桌面启动是否成功。
- 扫描是否成功。
- 每种格式结果。
- 最近活动记录语义是否正确。
- 源文件是否保持不变。
- git status 摘要。
```

---

## Task 108 Prompt：自动化回归与发布打包

```text
你正在执行“游戏资源管理器”v0.10.1 发布收口的 Task 108。

只执行 Task 108，不要创建 release 文档，不要改 README。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\package.ps1
3. I:\GameResManger\README.md 的 Verification 章节
4. I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md 中 Task 108

允许创建：
- I:\GameResManger\releases 下由 package.ps1 生成的版本产物目录

允许修改：
- I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

禁止：
- 不要手动删除旧 dist、target 或 releases 目录。
- 不要运行批量删除命令。
- 不要改版本号。
- 不要提交 Git。

必须按顺序运行：
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check
cd I:\GameResManger
npx tauri build
.\package.ps1

打包后必须记录：
1. 最新 releases 子目录路径。
2. 每个 artifact 的文件名。
3. 每个 artifact 的字节大小。
4. 每个 artifact 的 SHA-256。

可使用：
Get-ChildItem -LiteralPath .\releases -Directory | Sort-Object LastWriteTime -Descending | Select-Object -First 1
Get-Item -LiteralPath "<artifact>"
Get-FileHash -Algorithm SHA256 -LiteralPath "<artifact>"

完成后：
- 更新 Task 108 Notes。
- 不要创建 docs/releases/v0.10.1.md；这是 Task 109。
- 汇报所有命令结果、artifact 目录、大小、hash 和 git status。
```

---

## Task 109 Prompt：发布记录与 README 链接

```text
你正在执行“游戏资源管理器”v0.10.1 发布收口的 Task 109。

只执行 Task 109，不要重新打包，不要改代码。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\releases\v0.10.0.md
3. I:\GameResManger\docs\releases\v0.9.1.md
4. I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md 中 Task 109
5. Task 107 和 Task 108 的 Notes

前置条件：
- Task 108 已提供自动化验证结果、artifact 路径、大小、SHA-256。
- 如果 Task 107 没有真实 GUI 结果，release 文档必须明确写“GUI smoke pending/unavailable”，不得伪造通过。

允许创建：
- I:\GameResManger\docs\releases\v0.10.1.md

允许修改：
- I:\GameResManger\README.md
- I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

禁止：
- 不要修改 src 或 src-tauri 代码。
- 不要修改版本号。
- 不要提交 Git。

目标：
1. 创建 docs\releases\v0.10.1.md。
2. 写入 What's New、Verification 表、Release Artifacts 表、Desktop Smoke Note、Security And Safety。
3. 所有结果必须来自 Task 107-108 真实输出。
4. README.md 的 release record 链接从 v0.10.0 更新为 v0.10.1，并保留早期 release notes 目录入口。
5. 运行 git diff 审查文档变更。

必须运行：
cd I:\GameResManger
git rev-parse --short HEAD
git diff -- docs/releases/v0.10.1.md README.md tests/smoke/README.md docs/superpowers/plans/2026-06-17-v0.10.1-release-smoke-and-packaging.md

完成后汇报：
- release 文档路径。
- README 链接变更。
- 是否仍有 pending manual smoke。
- git diff 摘要。
```

---

## Task 110 Prompt：最终安全复核与交接

```text
你正在执行“游戏资源管理器”v0.10.1 发布收口的 Task 110。

这是最终复核任务。不要改代码，不要打包，不要提交。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md 中 Task 110
3. I:\GameResManger\docs\releases\v0.10.1.md
4. I:\GameResManger\src-tauri\tauri.conf.json

允许修改：
- I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md

必须运行：
cd I:\GameResManger
Select-String -LiteralPath .\src-tauri\tauri.conf.json -Pattern "assetProtocol|scope|media-src|asset-media|webview-v0.10.1" -Context 2,4
git status --short

检查点：
- assetProtocol.scope 只包含 $APPDATA/thumbnails/* 和 $APPDATA/thumbnails/**/*。
- CSP 只为媒体增加 asset-media，不放开任意本地路径。
- dataDirectory 是 webview-v0.10.1。
- working tree 只包含本阶段文档、smoke 记录、release artifacts（若未忽略）和用户已有未跟踪文件。
- 没有任何源素材文件被 Git 追踪或修改。
- 没有提交、tag、PR 或上传动作。

完成后更新 Task 110 Notes：
- Automated verification
- GUI smoke
- Packaging
- Release docs
- Remaining risks
- Git status

最终汇报：
- v0.10.1 是否已满足发布候选标准。
- 还缺哪些人工动作：例如提交、打 tag、上传 release artifact、最终安装包交互 smoke。
- 明确说明没有删除、移动、重命名、修改源素材文件。
```

---

## 推荐派发顺序

1. Task 107：派给能操作真实 Windows 桌面的 agent 或人工执行者。
2. Task 108：派给构建环境完整的 agent。
3. Task 109：派给文档 agent，必须读取 Task 107/108 结果。
4. Task 110：派给最终审查 agent。

Task 107 和 Task 108 不建议并行，因为 release 文档需要同时吸收真实 GUI 和打包证据。Task 109 必须在二者之后执行。
