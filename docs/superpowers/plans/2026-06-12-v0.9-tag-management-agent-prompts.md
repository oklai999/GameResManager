# v0.9 标签管理闭环 Agent Prompts

配套实施计划：

`I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md`

配套设计规格：

`I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md`

## 使用方式

- 每次只把一个任务提示词交给一个 agent。
- 必须按 Task 84 → Task 89 顺序执行。
- 后续任务开始前，先确认前一任务的验证已经通过。
- agent 不得自行开始下一个任务。
- 不要求 agent 创建 Git 提交，除非你明确要求。

---

## Task 84 Prompt：标签 Repository 生命周期

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 84。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 84

只执行 Task 84，不要提前执行 Task 85 或后续任务。

开始时：
- 运行 git status --short。
- 使用 rg 查找 Tag、list_tags、apply_tag_to_assets、refresh_asset_search_document 和相关测试。
- 阅读现有实现后再修改。

目标：
- 为 Rust Tag 模型增加 asset_count。
- 增加 #RRGGBB 标签颜色验证与大写规范化。
- list_tags 和 list_recent_tags 返回资源数量。
- 实现 update_tag，支持普通重命名、改色和重命名到已有标签时自动合并。
- 合并时保留目标标签颜色，资源关联不得重复。
- 实现 remove_tag_from_assets，支持单个/多个资源幂等解除。
- 实现 delete_tag，只删除 SQLite 标签和关联，不删除 assets，不触碰源文件。
- 所有标签变化都必须在事务中同步普通 FTS 和 trigram FTS。
- FTS 更新失败时整个操作必须回滚。

严格使用 TDD：
1. 按计划写失败测试。
2. 运行测试确认失败。
3. 写最小实现。
4. 运行验证。

测试数据库必须启用 SQLite foreign_keys，并为 asset_tags 配置生产一致的 ON DELETE CASCADE。

必须运行：
cd I:\GameResManger\src-tauri
cargo test tag_management_tests
cargo test fts_tests
cargo test tags::tests
cargo check

完成后：
- 不要执行批量删除命令。
- 不要修改任何原始素材文件。
- 不要回滚或覆盖已有用户改动。
- 不要开始 Task 85。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报修改文件、核心实现、测试结果及任何剩余风险。
```

---

## Task 85 Prompt：Tauri 与 TypeScript 契约

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 85。

工作目录：
I:\GameResManger

前置条件：
Task 84 已完成并通过验证。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 85

只执行 Task 85，不要提前执行 Task 86 或后续任务。

开始时：
- 运行 git status --short。
- 阅读 Task 84 已实现的 repository 函数和 Tag 模型。
- 检查 commands.rs、lib.rs、src/types/asset.ts、src/api/tauri.ts 的现有命名风格。

目标：
- 暴露 update_tag、remove_tag_from_assets、delete_tag Tauri commands。
- 在 lib.rs 注册 commands。
- TypeScript Tag 增加 asset_count。
- 增加 updateTag、removeTagFromAssets、deleteTag 类型化 API。
- 更新所有受影响的 Tag 测试数据，加入 asset_count。
- 保证 Rust snake_case 参数与前端 camelCase invoke 映射一致。

必须运行：
cd I:\GameResManger\src-tauri
cargo check
cd I:\GameResManger
npm run build

完成后：
- 不要开始 Task 86。
- 不要修改 Task 85 范围外的 UI 行为。
- 不要执行批量删除命令或修改源素材。
- 不要回滚用户改动。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报修改文件、命令契约、验证结果和剩余问题。
```

---

## Task 86 Prompt：标签管理器 UI

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 86。

工作目录：
I:\GameResManger

前置条件：
Task 84 和 Task 85 已完成并通过验证。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 86

只执行 Task 86，不要提前执行 Task 87 或后续任务。

开始时：
- 运行 git status --short。
- 阅读 CollectionManager、LibrarySidebar 及其测试和样式，复用现有交互模式。

目标：
- 新建 TagManager.tsx 和 TagManager.test.tsx。
- 显示标签名称、颜色和资源数量。
- 支持编辑名称和 #RRGGBB 颜色。
- 支持安全删除的二次确认。
- 确认文案必须明确不会删除或修改原始素材文件。
- 标签被合并或删除后，选中项自动回退到仍存在的标签。
- 空标签列表显示明确空状态。
- 在左侧栏增加“标签 / 管理”入口并打开 TagManager。
- 不新增路由。
- 尽量复用 CollectionManager 布局类，只添加必要的标签专用样式。

严格使用 TDD：
1. 先写 TagManager 和 LibrarySidebar 的失败测试。
2. 运行确认失败。
3. 实现最小 UI。
4. 运行验证。

必须运行：
npm test -- src/components/TagManager.test.tsx src/components/LibrarySidebar.test.tsx
npm run build

完成后：
- 不要开始 Task 87。
- 不要在此任务中串联真实 API 状态；该部分属于 Task 88。
- 不要执行批量删除命令或修改源素材。
- 不要回滚用户改动。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报 UI 行为、测试结果和任何交互风险。
```

---

## Task 87 Prompt：单选与多选解除标签

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 87。

工作目录：
I:\GameResManger

前置条件：
Task 84 至 Task 86 已完成并通过验证。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 87

只执行 Task 87，不要提前执行 Task 88 或 Task 89。

开始时：
- 运行 git status --short。
- 阅读 TagEditor、DetailsPanel 及其全部测试。

目标：
- TagEditor 增加必需的 onRemove(tagName) 属性。
- 每个已有标签 chip 提供可访问的“移除标签 标签名”按钮。
- 保持添加标签、最近标签和搜索建议行为不变。
- DetailsPanel 增加 onRemoveTag(tagName, assetIds)。
- 单选时解除当前资源标签。
- 多选时现有标签为共同标签，解除操作作用于全部所选资源。
- 增加 tagRefreshVersion，变化时重新加载当前标签。
- 更新所有测试 fixture，使新增 props 为显式必需参数。

严格使用 TDD：
1. 先写失败测试。
2. 运行确认失败。
3. 实现最小改动。
4. 运行验证。

必须运行：
npm test -- src/components/TagEditor.test.tsx src/components/DetailsPanel.test.tsx
npm run build

完成后：
- 不要开始 Task 88。
- 不要直接在组件中调用删除 repository；组件只通过 callbacks 通知 App。
- 不要执行批量删除命令或修改源素材。
- 不要回滚用户改动。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报组件契约、单选/多选行为和验证结果。
```

---

## Task 88 Prompt：App 状态串联与回归

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 88。

工作目录：
I:\GameResManger

前置条件：
Task 84 至 Task 87 已完成并通过验证。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 88

只执行 Task 88，不要提前执行 Task 89。

开始时：
- 运行 git status --short。
- 阅读现有 App.tsx 的 loadData、executeSearch、最近标签、标签应用和 toast 逻辑。
- 阅读 Task 85 API 与 Task 86/87 组件契约。

目标：
- App 增加全局 tags 和 tagRefreshVersion 状态。
- loadData 加载完整标签列表。
- 建立统一 refreshTagState，刷新 tags、recentTags 和详情标签版本。
- 添加标签后刷新标签状态与当前搜索。
- 实现标签重命名/改色/合并处理。
- 实现标签安全删除处理。
- 实现当前单选/多选资源解除标签处理。
- 通过标签名称解析 tag ID；找不到时显示明确错误。
- 更新 LibrarySidebar 和 DetailsPanel props。
- 更新全部测试 fixture。
- 所有成功和失败通过现有 toast 通道展示。

不要为了刷新标签而重置用户当前搜索、文件夹筛选、集合筛选或选中资源，除非现有行为确有必要。

必须运行：
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

如果测试失败，修复本任务造成的回归后再结束。

完成后：
- 不要开始 Task 89。
- 不要执行批量删除命令或修改源素材。
- 不要回滚用户改动。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报状态流、刷新策略、完整测试结果和剩余风险。
```

---

## Task 89 Prompt：版本、烟测与最终验证

```text
你正在执行“游戏资源管理器”v0.9 标签管理闭环的 Task 89。

工作目录：
I:\GameResManger

前置条件：
Task 84 至 Task 88 已完成，自动化测试已经通过。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-12-v0.9-tag-management-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-12-v0.9-tag-management.md 中的 Task 89

只执行 Task 89。

开始时：
- 运行 git status --short。
- 记录已有用户改动，不得回滚。

目标：
- 在 tests/smoke/README.md 添加 v0.9 标签管理烟测。
- 更新 README 的版本和标签管理能力说明。
- 将 package.json、package-lock.json、Cargo.toml、Cargo.lock、tauri.conf.json 统一为 0.9.0。
- 将 Tauri webview dataDirectory 更新为 webview-v0.9.0。
- 运行版本一致性测试。
- 运行完整前后端自动化验证。
- 尽量运行 npm run tauri dev 并按清单做 GUI 烟测。
- 验证标签重命名、颜色、自动合并、安全删除、单选解除、多选解除和搜索刷新。
- 验证所有源文件路径、内容和修改时间保持不变。

必须运行：
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

GUI 条件允许时运行：
cd I:\GameResManger
npm run tauri dev

如果 GUI、MSVC、Windows SDK 或权限导致某项无法执行：
- 不要修改业务代码规避环境问题。
- 记录准确错误和已经完成的替代验证。

完成前：
- 再次运行 git status --short。
- 确认没有删除、移动、重命名或修改任何原始素材文件。
- 不执行任何批量删除命令。
- 不清理或回滚无关改动。
- 除非明确要求，否则不要创建 Git 提交或发布版本。

最终汇报：
- 修改文件。
- 版本同步结果。
- 每条验证命令的结果。
- GUI 烟测结果或无法执行原因。
- 源文件安全结论。
- 剩余风险。
```
