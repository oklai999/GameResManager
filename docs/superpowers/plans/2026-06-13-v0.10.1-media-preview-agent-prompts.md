# v0.10.1 音视频基础预览 Agent Prompts

配套实施计划：

`I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md`

配套设计规格：

`I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md`

## 使用方式

- 必须在 v0.10.0 完成后执行。
- 严格按 Task 103 → 106 顺序执行。
- Task 103 是硬性原型门禁；桌面媒体 Range 请求不成立时必须停止。
- 不得通过扩大 asset protocol 全盘 scope、复制源文件或整文件读取大视频绕过问题。
- 默认不创建 Git 提交，除非你明确要求。

---

## 总控 Prompt：连续执行 v0.10.1

```text
你是“游戏资源管理器”项目的执行 Agent。请完整执行：
I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md

工作目录：
I:\GameResManger

硬性前置条件：
- v0.10.0 已完成。
- RecentActionType 已包含 preview_media。
- 最近活动时间线和 recordRecentAssetAction 已可用。
- 如果前置条件缺失，停止并报告。

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md

执行方式：
1. 使用 superpowers:executing-plans 或 superpowers:subagent-driven-development。
2. 严格按 Task 103 至 Task 106 顺序执行。
3. 使用 TDD。
4. Task 103 必须先完成 Rust 测试和桌面协议原型验证。
5. Task 103 原型不通过时，停止整个 v0.10.1，报告证据；不得继续写播放器 UI。
6. Task 106 前不得更新版本号为 0.10.1。
7. 除非用户明确要求，否则不要创建 Git 提交。

核心目标：
- 只按数据库资产 ID 提供媒体响应。
- 支持 MP3、WAV、OGG、MP4、WebM。
- 支持 HEAD 和单段 Range。
- 每段最多返回 1,024,000 bytes。
- 大于 64 MiB 的无 Range GET 返回 413，避免整文件内存读取。
- asset protocol scope 继续只允许缩略图目录。
- 右侧详情使用原生 audio/video controls 和 preload=metadata。
- 切换资源停止并释放旧媒体。
- 不自动播放。
- 同一连续播放会话只记录一次 preview_media；暂停后重新播放可记录新活动。
- 加载或编码失败显示回退，并保留打开、定位、复制和整理功能。

安全边界：
- 不接受前端传入任意绝对路径。
- 不使用 **/*、盘符根目录等全盘 scope。
- 不复制、转码、生成波形或修改源媒体。
- 不删除、移动或重命名任何素材。
- 禁止批量删除命令。

最终必须运行：
cd I:\GameResManger
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

必须运行桌面烟测：
cd I:\GameResManger
npm run tauri dev

完成后汇报：
- Task 103-106 状态。
- 自定义协议安全模型和 Range 证据。
- 五种格式的实际 WebView2 烟测结果。
- 播放生命周期与活动记录结果。
- 自动化验证结果。
- asset scope 复核。
- 原始素材安全结论。
- git status 摘要。
```

---

## Task 103 Prompt：受控媒体协议原型与 Range 响应

```text
你正在执行“游戏资源管理器”v0.10.1 的 Task 103。

这是硬性原型门禁。只执行 Task 103，不要提前实现 MediaPreview。

前置条件：
- v0.10.0 已完成。
- 如果未完成，停止并报告。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md 中的 Task 103
4. 本机 Tauri 2.11.2 register_asynchronous_uri_scheme_protocol 文档/源码
5. I:\GameResManger\src-tauri\tauri.conf.json

允许创建：
- I:\GameResManger\src-tauri\src\media.rs
- I:\GameResManger\src-tauri\tests\media_protocol.rs

允许修改：
- I:\GameResManger\src-tauri\src\db.rs
- I:\GameResManger\src-tauri\src\lib.rs
- I:\GameResManger\src-tauri\tauri.conf.json
- 主计划 Task 103 状态和 Notes

目标：
- media_mime 只允许 audio/mp3,wav,ogg 和 video/mp4,webm 的正确类型组合。
- get_asset_by_id 从数据库读取精确 Asset 行。
- URL 只包含正整数 asset_id，不包含绝对路径。
- 后端按 asset_id 获取数据库路径并只读打开。
- 拒绝未知 ID、is_missing、磁盘不存在、类型/扩展名不支持。
- 实现 HEAD 和单段 Range。
- Range 不合法或多段 Range 返回 416。
- 单段返回最多 1,024,000 bytes。
- 无 Range GET 仅允许文件 <= 64 MiB；更大返回 413。
- 使用实际文件 metadata 长度，不信任数据库 file_size。
- 注册 asset-media 异步自定义协议。
- Windows URL 固定为 http://asset-media.localhost/<asset_id>。
- CSP 增加 media-src，但 assetProtocol.scope 保持缩略图目录，不得扩大。

严格使用 TDD：
- MIME 白名单测试。
- 单段、开放结尾、越界和多段 Range 测试。
- 完整小文件响应。
- 206 partial content 与 Content-Range。
- HEAD 无 body。
- 404/415/416/413。
- 缺失、未知、不支持、未索引路径。

必须运行：
cd I:\GameResManger\src-tauri
cargo test media
cargo test --test media_protocol
cargo check

桌面原型门禁：
1. 使用一个真实 MP3 和一个真实 MP4。
2. 运行 npm run tauri dev。
3. 确认 WebView2 对媒体发起 metadata/Range 请求。
4. 确认 MP4 可以通过 206 分段响应拖动进度。
5. 确认大型媒体不会通过无 Range 整文件载入内存。

若门禁失败：
- 立即停止。
- 不开始 Task 104。
- 不扩大 asset scope。
- 不复制媒体到缓存。
- 不移除 64 MiB 内存保护。
- 不通过整文件 read_to_end 绕过。
- 报告请求头、响应状态、文件大小和失败现象，建议返回设计阶段修订。

若门禁通过：
- 更新 Task 103 Notes，记录真实 URL、Range 请求与响应证据。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报所有测试和桌面原型结果。
```

---

## Task 104 Prompt：MediaPreview 生命周期与失败回退

```text
你正在执行“游戏资源管理器”v0.10.1 的 Task 104。

硬性前置条件：
- Task 103 已完成。
- Rust 协议测试通过。
- MP3 和 MP4 桌面 Range 原型通过。
- 如果任一条件缺失，停止并报告，不要实现 UI。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md 中的 Task 104
3. I:\GameResManger\src\components\DetailsPanel.tsx
4. I:\GameResManger\src\styles.css

允许创建：
- I:\GameResManger\src\components\MediaPreview.tsx
- I:\GameResManger\src\components\MediaPreview.test.tsx

允许修改：
- I:\GameResManger\src\styles.css
- 主计划 Task 104 状态和 Notes

目标：
- MP3/WAV/OGG 渲染原生 audio controls。
- MP4/WebM 渲染原生 video controls。
- 使用 preload="metadata"。
- URL 为 http://asset-media.localhost/<asset_id>。
- mediaPreviewUrl 拒绝非正整数 ID。
- 非目标格式显示“此格式暂不支持应用内预览”。
- error 事件显示“当前文件或编码无法在应用内预览。”。
- 同一次连续播放只调用一次 onPlaybackStarted。
- pause 和 ended 后再次 play 可产生新调用。
- 切换资源或卸载时 pause、清除 src、load。
- 旧资源延迟事件不得记录为新资源。
- 不自动播放。
- 不生成波形，不添加自定义播放控件。

严格使用 TDD：
- 五种格式的元素类型、controls 和 preload。
- 不支持格式占位。
- 连续 play 去重。
- pause 后重新 play。
- asset 改变时 pause/load。
- error 回退。
- 旧资源延迟事件。

必须运行：
cd I:\GameResManger
npm test -- src/components/MediaPreview.test.tsx
npm run build

完成后：
- 不要修改 DetailsPanel 或 App；集成属于 Task 105。
- 不要改版本号。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报组件 props、生命周期、测试结果和浏览器环境限制。
```

---

## Task 105 Prompt：详情集成与预览活动记录

```text
你正在执行“游戏资源管理器”v0.10.1 的 Task 105。

前置条件：
- Tasks 103-104 已完成并通过验证。
- MediaPreview 已存在。
- 最近活动 API 已支持 preview_media。
- 如果缺失，停止并报告。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md 中的 Task 105
3. I:\GameResManger\src\components\DetailsPanel.tsx
4. I:\GameResManger\src\components\DetailsPanel.test.tsx
5. I:\GameResManger\src\App.tsx

只执行 Task 105，不要提前执行 Task 106。

允许修改：
- I:\GameResManger\src\components\DetailsPanel.tsx
- I:\GameResManger\src\components\DetailsPanel.test.tsx
- I:\GameResManger\src\App.tsx
- 因新增必需 props 需要修改的测试 fixtures
- 主计划 Task 105 状态和 Notes

目标：
- DetailsPanel 增加必需 callback onMediaPlaybackStarted(asset)。
- 目标音视频格式使用 MediaPreview。
- 图片继续使用现有缩略图。
- 其他资源继续使用现有占位。
- 媒体失败时仍保留打开文件、打开目录、复制路径、备注、标签和集合。
- App 在播放开始时非阻塞写入 preview_media。
- 活动写入失败只 console.error，不显示 toast，不停止播放。
- 最近工作区可见时，成功记录后刷新时间线。
- 不因选择资源写入预览活动。
- 不因 metadata、canplay、seeking、volumechange 写入活动。

严格使用 TDD：
- 支持音频进入 MediaPreview。
- 支持视频进入 MediaPreview。
- 失败后文件动作仍存在。
- onMediaPlaybackStarted 正确透传。
- App 仅记录 preview_media。
- 记录失败不影响 UI/播放。

必须运行：
cd I:\GameResManger
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

完成后：
- 不改版本号。
- 不开始 Task 106。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报详情选择逻辑、非阻塞记录策略、完整验证结果和剩余风险。
```

---

## Task 106 Prompt：五格式桌面烟测与发布验证

```text
你正在执行“游戏资源管理器”v0.10.1 的 Task 106。

前置条件：
- Tasks 103-105 已完成。
- 自动化测试通过。
- 自定义媒体协议原型已通过。
- 如果任一条件缺失，停止并报告，不要改版本号。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md 中的 Task 106
3. I:\GameResManger\README.md
4. I:\GameResManger\tests\smoke\README.md
5. 版本一致性测试

只执行 Task 106。

目标：
- 添加 MP3、WAV、OGG、MP4、WebM 和不兼容编码文件的 smoke matrix。
- README 更新音视频预览说明和限制。
- 所有发布元数据同步为 0.10.1。
- dataDirectory 改为 webview-v0.10.1。
- 运行完整自动化验证。
- 必须运行 tauri dev 桌面烟测。
- 记录每种格式的 metadata、播放、暂停、进度、音量和切换停止结果。
- 验证首次 play 记录一次 preview_media。
- 验证同一连续播放不重复记录。
- 验证 pause/play 产生新活动。
- 验证只选中不记录。
- 验证编码失败显示回退并保留文件动作。
- 验证 assetProtocol.scope 仍只包含缩略图目录。
- 验证源文件路径、内容、大小和修改时间不变。

必须运行：
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check
cd I:\GameResManger
npm run tauri dev

如果某格式因 WebView2 编码支持失败：
- 这不自动等于代码失败。
- 确认进入明确回退状态。
- 记录容器、编码和错误表现。
- 不添加 ffmpeg，不自动转码，不扩大权限。

完成后：
- 更新主计划 Task 103-106 状态和 Notes。
- 除非明确要求，否则不要创建 Git 提交或打包发布。
- 汇报版本同步、全部验证、格式矩阵、Range 行为、权限复核、安全结论和 git status。
```

---

## 推荐派发顺序

1. Task 103：必须独立执行并完成桌面原型门禁。
2. Task 104：仅在 Task 103 通过后执行。
3. Task 105：等待 Task 104。
4. Task 106：最终发布验证。

四个任务存在严格依赖，不允许并行执行。

