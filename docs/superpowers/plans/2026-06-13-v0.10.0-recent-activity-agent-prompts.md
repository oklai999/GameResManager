# v0.10.0 最近活动时间线 Agent Prompts

配套实施计划：

`I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md`

配套设计规格：

`I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md`

## 使用方式

- 推荐每次只把一个 Task Prompt 交给一个 agent。
- 严格按 Task 97 → 102 顺序执行。
- 后续任务开始前，确认前置任务要求的验证已经通过。
- 每个 agent 只执行被分配的任务，不得自行进入后续任务。
- 默认不创建 Git 提交，除非你明确要求。
- 若工作区存在用户改动，必须保留并与之协作，不得回滚。

---

## 总控 Prompt：连续执行 v0.10.0

```text
你是“游戏资源管理器”项目的执行 Agent。请完整执行：
I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md

执行方式：
1. 使用 superpowers:executing-plans 或 superpowers:subagent-driven-development。
2. 严格按 Task 97 至 Task 102 顺序执行。
3. 每个 Task 使用 TDD：先写失败测试，运行确认失败，再写最小实现并验证。
4. 每完成一个 Task，立即运行该任务要求的验证并更新任务板状态与 Notes。
5. 测试失败时使用 superpowers:systematic-debugging 查明根因，不跳过测试，不削弱断言。
6. Task 102 之前不得把版本号更新为 0.10.0。
7. 除非用户明确要求，否则不要创建 Git 提交。

核心目标：
- 把现有 recentAssetIds 临时拼装改为专用最近活动分页接口。
- 活动支持 open_file、reveal_folder、copy_path、preview_media 类型契约。
- 历史自动保留最近 30 天且全局最多 1,000 条。
- 最近工作区使用按资源折叠的活动时间线。
- 支持全部、今天、最近 7 天和动作类型筛选。
- 同一资源可展开查看活动明细。
- 缺失资源保留历史并限制不安全动作。
- 其他工作区继续使用现有虚拟资源网格。

安全边界：
- 不删除、移动、重命名或修改任何原始素材文件。
- 历史清理只能删除 SQLite recent_asset_actions 行。
- 不向素材目录写入文件。
- 禁止 del /s、rd /s、rmdir /s、Remove-Item -Recurse、rm -rf。
- 禁止 git reset --hard、git checkout --。
- 不回滚或覆盖已有用户改动。

最终必须运行：
cd I:\GameResManger
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

GUI 条件允许时运行：
cd I:\GameResManger
npm run tauri dev

完成后汇报：
- Task 97-102 状态。
- 修改和新增文件。
- 活动清理、聚合、筛选、分页和前端状态流摘要。
- 每条验证命令的实际结果。
- GUI 烟测结果或无法执行原因。
- 原始素材安全结论。
- 当前 git status --short 摘要。
```

---

## Task 97 Prompt：活动 Schema、类型与保留规则

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 97。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 97

只执行 Task 97，不要提前执行 Task 98 或后续任务。

开始时：
- 运行 git status --short。
- 使用 rg 查找 recent_asset_actions、RecentAssetAction、record_recent_asset_action 和现有测试。
- 阅读 migration 0005、models.rs、db.rs 后再修改。

允许修改：
- I:\GameResManger\src-tauri\migrations\0009_recent_activity_timeline.sql
- I:\GameResManger\src-tauri\src\models.rs
- I:\GameResManger\src-tauri\src\db.rs
- 主计划 Task 97 的状态和 Notes

目标：
- 新增 migration 0009，重建 recent_asset_actions 的 CHECK 约束。
- 允许 open_file、reveal_folder、copy_path、preview_media。
- 创建 created_at、asset+created_at、type+created_at 索引。
- 增加统一 action type 验证。
- 增加可注入时间的 record_recent_asset_action_at。
- 写入活动和清理必须处于同一 SQLite 事务。
- 删除早于当前时间 30 天的记录。
- 按 created_at DESC、id DESC 只保留最新 1,000 条。
- 保留 record_recent_asset_action 的现有对外入口。

严格使用 TDD：
1. 按计划写 accepts_all_supported_activity_types。
2. 写 rejects_unknown_activity_type。
3. 写 cleanup_removes_old_rows_and_keeps_latest_thousand。
4. 运行测试确认失败。
5. 实现最小代码。
6. 再运行验证。

注意：
- cleanup 只能删除数据库活动行，不得接触磁盘文件。
- 使用稳定排序处理相同时间戳。
- 不修改 commands、前端、UI 或版本号。

必须运行：
cd I:\GameResManger\src-tauri
cargo test recent_activity_tests
cargo check

完成后：
- 不要开始 Task 98。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报 migration 策略、事务边界、测试结果和剩余风险。
- 给出建议写入 Task 97 Notes 的一句话。
- 再次运行并汇报 git status --short。
```

---

## Task 98 Prompt：活动聚合、筛选与分页

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 98。

前置条件：
- Task 97 已完成并通过 cargo test recent_activity_tests 和 cargo check。
- recent_asset_actions 已允许 preview_media。
- 如果前置条件缺失，停止并报告，不要自行重做 Task 97。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\specs\2026-06-13-v0.10-recent-activity-media-preview-design.md
3. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 98

只执行 Task 98，不要提前执行 Task 99。

允许修改：
- I:\GameResManger\src-tauri\src\models.rs
- I:\GameResManger\src-tauri\src\db.rs
- 主计划 Task 98 的状态和 Notes

目标：
- 定义 RecentActivityRequest、RecentActivityItem、RecentActivityResponse。
- 实现按资源聚合的最近活动分页查询。
- 支持 period：all、today、week。
- today 使用调用时固定时区的当天零点；测试不得依赖测试机器时区。
- week 表示滚动最近 7 天。
- 支持 action_type：all 或四种动作。
- total_count 是过滤后去重资源数量。
- 聚合项按 latest_action_at DESC 和资源 ID 稳定排序。
- 返回四种动作计数及最新动作类型。
- 每个资源最多返回最新 50 条明细，但计数统计完整筛选范围。
- limit 限制 1-100，offset 不得为负。
- 缺失资源仍可查询。

严格使用 TDD：
- groups_actions_by_asset_with_counts_and_details
- filters_today_week_and_action_type
- keeps_missing_assets_and_uses_stable_pagination
- 补充同时间戳 ID 稳定排序测试
- 补充单资源明细最多 50 条但计数完整的测试

实现要求：
- 使用 QueryBuilder<Sqlite> 绑定动态条件，不拼接用户输入。
- 查询分为受限的聚合页和该页资源明细，不做无上限全表加载。
- Asset 查询字段必须与生产 Asset 模型完全一致。

必须运行：
cd I:\GameResManger\src-tauri
cargo test recent_activity_tests
cargo check

完成后：
- 不要开始 Task 99。
- 不修改 commands、前端或版本号。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报 SQL/QueryBuilder 结构、时区语义、分页结果和测试结果。
- 给出 Task 98 Notes 建议。
```

---

## Task 99 Prompt：Tauri Command 与 TypeScript 契约

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 99。

前置条件：
- Tasks 97-98 已完成并通过验证。
- Rust 已存在 list_recent_activity repository 和对应模型。
- 如果缺失，停止并报告。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 99
3. I:\GameResManger\src-tauri\src\commands.rs
4. I:\GameResManger\src-tauri\src\lib.rs
5. I:\GameResManger\src\types\asset.ts
6. I:\GameResManger\src\api\tauri.ts

只执行 Task 99，不要提前执行 Task 100。

允许修改：
- I:\GameResManger\src-tauri\src\commands.rs
- I:\GameResManger\src-tauri\src\lib.rs
- I:\GameResManger\src\types\asset.ts
- I:\GameResManger\src\api\tauri.ts
- 因类型变化必须修复的测试数据
- 主计划 Task 99 的状态和 Notes

目标：
- record_recent_asset_action command 委托 repository 验证，不重复维护白名单。
- 新增并注册 list_recent_activity command。
- 应用启动后非阻塞执行 cleanup_recent_asset_actions。
- TypeScript 定义 RecentActionType、RecentActivityPeriod、Request、Item、Response。
- 新增 listRecentActivity wrapper。
- 移除 App 对旧 listRecentAssetActions 的依赖；本任务只改 API 契约，不改 App 行为。
- Rust snake_case 与前端 invoke camelCase 映射保持现有惯例。

必须运行：
cd I:\GameResManger\src-tauri
cargo check
cd I:\GameResManger
npm run build

完成后：
- 不要实现时间线 UI。
- 不要改版本号。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报 command、类型字段、启动清理和验证结果。
- 给出 Task 99 Notes 建议。
```

---

## Task 100 Prompt：最近筛选侧栏与时间线组件

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 100。

前置条件：
- Tasks 97-99 已完成。
- TypeScript 已存在 RecentActivityItem 等类型。
- 如果前置条件缺失，停止并报告。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 100
3. I:\GameResManger\src\components\LibrarySidebar.tsx
4. I:\GameResManger\src\components\LibrarySidebar.test.tsx
5. I:\GameResManger\src\components\AssetGrid.tsx
6. I:\GameResManger\src\components\EmptyState.tsx
7. I:\GameResManger\src\styles.css

只执行 Task 100，不要提前执行 Task 101。

允许创建：
- I:\GameResManger\src\components\RecentActivityTimeline.tsx
- I:\GameResManger\src\components\RecentActivityTimeline.test.tsx

允许修改：
- I:\GameResManger\src\components\LibrarySidebar.tsx
- I:\GameResManger\src\components\LibrarySidebar.test.tsx
- I:\GameResManger\src\components\EmptyState.tsx
- I:\GameResManger\src\styles.css
- 主计划 Task 100 的状态和 Notes

目标：
- 最近侧栏显示时间范围：全部、今天、最近 7 天。
- 最近侧栏显示动作类型：全部、打开文件、打开所在目录、复制路径、媒体预览。
- 不再通过 onFilterChange("recent") 选择最近工作区。
- 新建 RecentActivityTimeline。
- 每个资源只显示一张活动卡。
- 卡片显示文件名、路径摘要、最后动作、相对时间、动作摘要、收藏和缺失状态。
- 单击卡片选择资源。
- 展开明细不改变选择。
- 双击可用资源调用打开文件；缺失资源不得打开。
- 加载更多通过 callback 触发。
- 添加“暂无最近活动”和“当前筛选没有活动”空状态。

严格使用 TDD：
- 选择资源但不展开
- 展开明细独立于选择
- 双击打开与缺失禁用
- 加载更多
- 侧栏时间和动作筛选

组件只使用 callbacks，不直接调用 Tauri API。

必须运行：
npm test -- src/components/RecentActivityTimeline.test.tsx src/components/LibrarySidebar.test.tsx
npm run build

完成后：
- 不要修改 App 状态流。
- 不要改版本号。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报组件契约、无障碍名称、测试和构建结果。
- 给出 Task 100 Notes 建议。
```

---

## Task 101 Prompt：App 最近工作区串联

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 101。

前置条件：
- Tasks 97-100 已完成。
- listRecentActivity API 和 RecentActivityTimeline 已存在。
- 如果缺失，停止并报告。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 101
3. I:\GameResManger\src\App.tsx
4. I:\GameResManger\src\components\RecentActivityTimeline.tsx
5. I:\GameResManger\src\components\LibrarySidebar.tsx

只执行 Task 101，不要提前执行 Task 102。

允许修改：
- I:\GameResManger\src\App.tsx
- 因新增必需 props 需要修复的测试 fixtures
- 主计划 Task 101 的状态和 Notes

目标：
- 删除 recentAssetIds 状态和旧 listRecentAssetActions 加载。
- 新增 recentPeriod、recentActionType、recentItems、recentTotalCount、loading 状态。
- 仅在 activeSection === "recent" 时查询最近活动。
- 筛选变化时清空选择、重置分页并重新查询。
- 实现最近活动加载更多。
- 最近工作区渲染 RecentActivityTimeline；其他工作区继续渲染现有搜索工具栏和 AssetGrid。
- 最近工作区隐藏 SearchToolbar、FilterPanel、ActiveFilterChips 和资源网格 footer。
- 右侧 DetailsPanel 保持可用。
- 文件打开、定位和复制成功后，活动记录失败不得让已成功动作显示失败。
- 最近工作区可见时，成功记录活动后刷新时间线。
- 不破坏搜索、扫描、标签、集合和收藏状态。

重要错误边界：
- 文件系统动作的 try/catch 与 recordRecentAssetAction 的非阻塞 catch 分离。
- 不因活动记录失败显示“打开文件失败”或“复制路径失败”。

必须运行：
cd I:\GameResManger
npm test
npm run build

完成后：
- 不要改版本号。
- 不要开始 Task 102。
- 除非明确要求，否则不要创建 Git 提交。
- 汇报移除的旧状态、活动加载流程、非阻塞记录策略和完整前端验证结果。
- 给出 Task 101 Notes 建议。
```

---

## Task 102 Prompt：烟测、版本同步与发布验证

```text
你正在执行“游戏资源管理器”v0.10.0 的 Task 102。

前置条件：
- Tasks 97-101 已完成并通过验证。
- 最近活动时间线已经可用。
- 如果任一前置任务未完成，停止并报告，不要提前改版本号。

工作目录：
I:\GameResManger

开始前必须完整阅读：
1. I:\GameResManger\AGENTS.md
2. I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity.md 中的 Task 102
3. I:\GameResManger\README.md
4. I:\GameResManger\tests\smoke\README.md
5. 版本一致性相关测试

只执行 Task 102。

目标：
- 补充 v0.10.0 最近活动烟测清单。
- README 更新当前版本和最近活动能力。
- 同步 package.json、package-lock.json、Cargo.toml、Cargo.lock、tauri.conf.json 为 0.10.0。
- dataDirectory 更新为 webview-v0.10.0。
- 先运行元数据测试，再运行完整前后端验证。
- GUI 可用时运行 tauri dev，验证折叠、筛选、分页、选择、详情、缺失状态和双击打开。
- 验证原始素材文件没有被删除、移动、重命名或修改。
- 验证历史清理只影响 SQLite 活动记录。
- 全部通过后更新主计划 Task 97-102 状态和 Notes。

必须运行：
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
npm test
npm run build
cd I:\GameResManger\src-tauri
cargo test
cargo check

GUI 条件允许时：
cd I:\GameResManger
npm run tauri dev

如果 GUI、MSVC、Windows SDK 或权限导致验证失败：
- 不修改业务代码规避环境问题。
- 记录准确错误和替代验证。

完成后：
- 除非明确要求，否则不要创建 Git 提交或发布安装包。
- 汇报版本同步、测试数量与结果、GUI 结果、安全结论、剩余风险和 git status。
```

---

## 推荐派发顺序

1. Task 97
2. Task 98
3. Task 99
4. Task 100
5. Task 101
6. Task 102

本计划任务存在连续的数据与契约依赖，不建议并行执行。

