# v0.7.0 CJK Substring Search Agent Prompts

用途：把 `2026-06-10-v0.7-cjk-substring-search.md` 转换成可直接交给其他 Agent 执行的提示词。

## 通用约束

- 默认工作目录：`I:\GameResManger`。
- 默认使用中文回复。
- 开始前必须读取：
  - `I:\GameResManger\AGENTS.md`
  - `I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md`
  - `G:\oklai999的策划仓库\游戏资源管理器\需求整理_v0.1.md`
- 开始和结束时运行 `git status --short`。
- 当前工作区已有 v0.6 相关未提交改动。不得回滚、覆盖、清理或顺手格式化无关文件。
- 不删除、移动、重命名或修改原始素材文件。
- 不向素材目录写入数据库、索引、缩略图、缓存、元数据或生成文件。
- 禁止批量删除命令：`del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
- 不执行 `git reset --hard`、`git checkout --`。
- 不添加 AI 打标、删除、批量移动、批量重命名、图集、云同步或完整运行时预览。
- 使用 TDD：先添加或确认失败测试，再实现最小代码，再运行验证。
- 不创建 Git 提交，除非用户明确要求。
- 完成任务后更新主计划中对应任务的状态与 Notes；如果只负责代码且没有权限更新计划，应在回复中明确建议的状态备注。

---

## 总控 Prompt：连续执行 v0.7.0

```text
你是本项目的执行 Agent。请完整执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md

目标：
完成 v0.7.0 CJK 中文子串搜索与查询加固。中文关键词应能匹配文件名、路径、备注和标签中间的文本，同时保持搜索范围、筛选、排序、分页和 total_count 一致。

执行方式：
1. 使用 superpowers:executing-plans 或 superpowers:subagent-driven-development。
2. 严格按 Task 71 到 Task 77 顺序执行。
3. 每完成一个 Task，立即运行该任务要求的验证，并更新任务板。
4. 遇到测试失败时先定位根因，不要跳过测试或削弱断言。
5. Task 77 之前不要把版本号改为 0.7.0。

关键实现边界：
- 保留现有 asset_search_fts unicode61 前缀索引。
- 新增 asset_search_trigram_fts，三字符及以上关键词走 trigram。
- 一到两个 Unicode 字符的关键词使用参数化、按搜索范围限制的 LIKE 回退。
- 多关键词使用 AND；每个关键词可在任一启用范围内匹配。
- 用户输入中的 SQL/FTS 操作符、%、_、引号必须按字面值处理。
- row 查询和 count 查询必须共享同一条件及 bind 顺序。
- 扫描、备注、标签、移除文件夹索引时同步两个 FTS 表。
- 所有新增数据只写入应用 SQLite。

最终必须运行：
- npm test
- npm run build
- cd I:\GameResManger\src-tauri
- cargo test
- cargo check

GUI 可用时再运行：
- npm run tauri dev

完成后回复：
- Task 71-77 的完成状态。
- 新增/修改文件清单。
- 搜索策略摘要。
- migration 与旧数据库兼容结果。
- 前端测试、构建、Rust 测试和 cargo check 的实际结果。
- GUI 烟测是否执行；未执行需说明原因。
- 当前 git status 摘要。
```

---

## Prompt 1：Task 71 - 固化中文子串行为

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 71。

目标：
用失败测试定义 v0.7.0 中文子串搜索行为，不实现生产搜索逻辑。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src-tauri\src\search.rs
- I:\GameResManger\src-tauri\migrations\0007_asset_search_fts.sql

只允许修改：
- I:\GameResManger\src-tauri\src\search.rs
- 主计划文件中 Task 71 的状态和备注

执行要求：
1. 运行 git status --short，保留所有无关改动。
2. 创建或扩展 cjk_search_pool 测试 fixture。
3. fixture 同时创建 unicode61 和 trigram FTS 测试表。
4. 添加以下测试：
   - cjk_substring_matches_middle_of_note
   - cjk_substring_matches_middle_of_file_name
   - cjk_substring_respects_selected_scope
   - multiple_substring_terms_use_and_semantics
5. 测试数据至少覆盖：
   - 备注：主角待机动画
   - 文件名：森林背景树.png
   - 路径：/test/角色/hero.png
   - 标签：角色、动画
6. 运行：
   cd I:\GameResManger\src-tauri
   cargo test search::tests::cjk_substring
   cargo test search::tests::multiple_substring_terms_use_and_semantics
7. 至少确认“待机”匹配“主角待机动画”的测试在 v0.6 前缀实现下失败。
8. 不要实现 trigram migration，不要修改生产查询逻辑。

完成后回复：
- 修改文件。
- 新增测试名称。
- 哪个测试按预期失败及失败原因。
- git status 摘要。
```

---

## Prompt 2：Task 72 - 添加 trigram migration

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 72。

前置条件：
- src-tauri/migrations/0007_asset_search_fts.sql 已存在。
- Task 71 的测试已添加。
- 如果 0008 编号已被其他 migration 占用，停止并报告，不要覆盖。

目标：
创建 asset_search_trigram_fts，并从已有 assets、note、tags 数据回填，旧数据库无需重新扫描素材目录。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src-tauri\migrations\0007_asset_search_fts.sql
- I:\GameResManger\src-tauri\src\db.rs

只允许创建：
- I:\GameResManger\src-tauri\migrations\0008_asset_search_trigram_fts.sql

只允许修改：
- I:\GameResManger\src-tauri\src\db.rs
- 主计划文件中 Task 72 的状态和备注

执行要求：
1. 运行 git status --short。
2. 先添加 migration_0008_backfills_trigram_fts_from_existing_assets 测试。
3. 创建 FTS5 表：
   asset_search_trigram_fts(file_name, absolute_path, note, tags, tokenize='trigram')
4. migration 必须回填所有现有 assets，标签按名称排序后用空格连接。
5. migration 只能操作应用数据库，不读取或修改源素材文件。
6. 运行：
   cd I:\GameResManger\src-tauri
   cargo test db::tests::migration_0008
   cargo check
7. 不要实现搜索查询，不要改前端。

完成后回复：
- migration 路径和 SQL 行为。
- 回填测试结果。
- cargo check 结果。
- git status 摘要。
```

---

## Prompt 3：Task 73 - 同步双 FTS 索引

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 73。

前置条件：
- Task 72 已完成。
- asset_search_fts 和 asset_search_trigram_fts 均存在。

目标：
让资产扫描、备注修改、标签修改和资源库索引移除事务性同步两个 FTS 表。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src-tauri\src\db.rs
- I:\GameResManger\src-tauri\src\scan_service.rs

只允许修改：
- I:\GameResManger\src-tauri\src\db.rs
- I:\GameResManger\src-tauri\src\scan_service.rs
- 主计划文件中 Task 73 的状态和备注

执行要求：
1. 运行 git status --short。
2. 添加常量 PREFIX_FTS_TABLE 和 TRIGRAM_FTS_TABLE。
3. 抽取 replace_search_document，只接受模块内固定表名调用；不得使用用户输入作为表名。
4. refresh_asset_search_document 必须在同一事务中更新两个表。
5. 资产不存在时必须清除两个表的 rowid。
6. scan_service 批量写入后必须删除并重建两个表对应文档。
7. delete_library_folder 删除应用索引前必须清理两个 FTS 表，但不得操作源文件夹。
8. 添加并运行：
   - update_asset_note_refreshes_trigram_fts
   - apply_tag_refreshes_trigram_fts
   - 扩展 rescan_preserves_note_and_tag_in_fts
9. 运行：
   cd I:\GameResManger\src-tauri
   cargo test db::tests::update_asset_note_refreshes_trigram_fts
   cargo test db::tests::apply_tag_refreshes_trigram_fts
   cargo test scan_service::tests::rescan_preserves_note_and_tag_in_fts
   cargo check
10. 不要实现查询策略，不要改前端。

完成后回复：
- 修改文件。
- 双索引同步触发点。
- 事务和回滚行为。
- 测试及 cargo check 结果。
- git status 摘要。
```

---

## Prompt 4：Task 74 - 实现混合子串查询

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 74。

前置条件：
- Task 71-73 已完成。
- asset_search_trigram_fts migration 和同步逻辑已存在。

目标：
实现统一的混合搜索计划：
- 三字符及以上：trigram FTS。
- 一到两字符：参数化、范围受限的 LIKE。
- row 和 count 共用条件及 bind 顺序。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src-tauri\src\search.rs
- I:\GameResManger\src-tauri\src\models.rs

只允许修改：
- I:\GameResManger\src-tauri\src\search.rs
- 主计划文件中 Task 74 的状态和备注

执行要求：
1. 运行 git status --short。
2. 实现 KeywordTerm、SearchBind、SearchWhere。
3. classify_terms 按 Unicode char 数量分类，不要按字节长度。
4. 短词 LIKE 必须转义反斜杠、%、_，并使用 ESCAPE '\'。
5. 标签短词必须通过 asset_tags + tags 的 EXISTS 子查询匹配。
6. trigram FTS 表达式只包含启用范围。
7. 每个 term 独立成为一个 AND 条件；同一 term 的启用范围使用 OR。
8. 非空查询且无启用范围时返回零结果。
9. 保留现有排序白名单、分页上限、所有过滤条件和系统元数据排除。
10. row 查询和 count 查询必须调用同一个 build_search_where。
11. 添加/保留测试：
    - 中文备注中间子串
    - 中文文件名中间子串
    - 搜索范围隔离
    - 多词 AND
    - %、_、引号、FTS 操作符字面量
    - count 与 rows 一致
12. 运行：
    cd I:\GameResManger\src-tauri
    cargo test search
    cargo check

禁止事项：
- 不要把用户输入直接拼入 SQL。
- 不要对三字符及以上词退化为 assets 列上的全表 LIKE。
- 不要改变 Tauri command 或前端类型契约。

完成后回复：
- 搜索策略摘要。
- 修改文件。
- 测试结果和测试数量。
- cargo check 结果。
- git status 摘要。
```

---

## Prompt 5：Task 75 - 添加搜索提示

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 75。

前置条件：
- Task 74 已完成并通过 cargo test search。

目标：
在搜索栏旁用克制文案说明中文片段和多关键词语义，不增加搜索模式或设置。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src\components\SearchToolbar.tsx
- I:\GameResManger\src\components\SearchToolbar.test.tsx
- I:\GameResManger\src\styles.css

只允许修改：
- I:\GameResManger\src\components\SearchToolbar.tsx
- I:\GameResManger\src\components\SearchToolbar.test.tsx
- I:\GameResManger\src\styles.css
- 主计划文件中 Task 75 的状态和备注

执行要求：
1. 运行 git status --short。
2. 先添加测试，断言出现：
   支持中文片段；空格分隔的关键词需同时匹配
3. 使用 search-query-group 和 search-hint，不修改输入框 aria-label。
4. 文案字号保持 11px，颜色使用次要文本色，不增加弹窗或帮助页面。
5. 保持搜索范围、高级筛选和排序交互不变。
6. 运行：
   npm test -- src/components/SearchToolbar.test.tsx
   npm run build

完成后回复：
- 修改文件。
- UI 文案。
- 测试和构建结果。
- git status 摘要。
```

---

## Prompt 6：Task 76 - 查询策略与性能烟测

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 76。

前置条件：
- Task 74 已完成。

目标：
锁定查询策略，防止三字符及以上关键词意外退化为宽泛 LIKE，并补齐手动大库搜索检查。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\src-tauri\src\search.rs
- I:\GameResManger\tests\smoke\README.md
- I:\GameResManger\README.md

只允许修改：
- I:\GameResManger\src-tauri\src\search.rs
- I:\GameResManger\tests\smoke\README.md
- I:\GameResManger\README.md
- 主计划文件中 Task 76 的状态和备注

执行要求：
1. 运行 git status --short。
2. 添加 test-only SQL builder，不新增生产 command。
3. 添加测试：
   - query_plan_uses_trigram_for_three_character_term
   - query_plan_limits_short_like_to_enabled_scope
4. 三字符测试必须断言 SQL 包含 asset_search_trigram_fts MATCH，且不包含 assets.file_name LIKE。
5. 两字符测试必须断言只对启用范围生成 LIKE。
6. 在 smoke README 添加 v0.7.0 CJK Substring Search Smoke Test，覆盖：
   - 待机匹配主角待机动画
   - 背景树匹配森林背景树.png
   - 范围隔离
   - 多词 AND
   - 特殊字符
   - 20,000 资源大库响应
   - 修改备注/标签后立即搜索
   - 重扫后搜索
   - 移除索引不删除源文件
7. README 将 prefix-only 限制改为：
   三字符及以上走 trigram；一到两字符使用范围受限的字面量回退，超大库可能较慢。
8. 运行：
   cd I:\GameResManger\src-tauri
   cargo test search::tests::query_plan
   cargo test search

完成后回复：
- 新增查询策略测试。
- README 与 smoke 文档变化。
- 测试结果。
- 尚未执行的手动烟测。
- git status 摘要。
```

---

## Prompt 7：Task 77 - 版本同步与最终验证

```text
你是 Codex 子代理，请只执行：
I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
中的 Task 77。

前置条件：
- Task 71-76 已完成。
- cargo test search、前端目标测试和 npm run build 已通过。
- 如果任一前置任务未完成，停止并列出缺失项，不要提前改版本号。

目标：
将应用版本统一为 0.7.0，并执行完整自动化验证。

必须先读：
- I:\GameResManger\AGENTS.md
- I:\GameResManger\docs\superpowers\plans\2026-06-10-v0.7-cjk-substring-search.md
- I:\GameResManger\package.json
- I:\GameResManger\package-lock.json
- I:\GameResManger\src-tauri\Cargo.toml
- I:\GameResManger\src-tauri\Cargo.lock
- I:\GameResManger\src-tauri\tauri.conf.json
- I:\GameResManger\README.md
- I:\GameResManger\tests\viteCacheBust.test.ts
- I:\GameResManger\tests\tauriWebviewCache.test.ts

只允许修改：
- I:\GameResManger\package.json
- I:\GameResManger\package-lock.json
- I:\GameResManger\src-tauri\Cargo.toml
- I:\GameResManger\src-tauri\Cargo.lock
- I:\GameResManger\src-tauri\tauri.conf.json
- I:\GameResManger\README.md
- 主计划文件中 Task 77 的状态和备注

执行要求：
1. 运行 git status --short。
2. 将上述所有版本元数据统一为 0.7.0。
3. 确认 Tauri dataDirectory 仍由版本派生为 webview-v0.7.0。
4. 运行：
   npm test -- tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
   npm test
   npm run build
   cd I:\GameResManger\src-tauri
   cargo test
   cargo check
5. GUI 可用时运行：
   cd I:\GameResManger
   npm run tauri dev
6. GUI 烟测至少检查：
   - migration 无错误
   - 旧数据仍可见
   - 待机匹配主角待机动画
   - 筛选、排序、分页、虚拟网格、标签、备注和安全文件动作正常
7. 不要打包或发布 release，除非用户明确要求。
8. 不要创建提交，除非用户明确要求。

完成后回复：
- 版本同步文件清单。
- 每条验证命令的实际结果。
- GUI 烟测结果或未执行原因。
- 未解决风险。
- 最终 git status 摘要。
```

---

## 建议派发顺序

1. Prompt 1：Task 71
2. Prompt 2：Task 72
3. Prompt 3：Task 73
4. Prompt 4：Task 74
5. Prompt 5：Task 75
6. Prompt 6：Task 76
7. Prompt 7：Task 77

Task 71-74 修改同一 Rust 搜索链路，必须串行执行。Task 75 可在 Task 74 完成后独立执行。Task 76 依赖 Task 74 的最终查询结构。Task 77 必须最后执行。
