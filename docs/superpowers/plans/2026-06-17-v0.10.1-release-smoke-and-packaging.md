# v0.10.1 Release Smoke And Packaging Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the remaining v0.10.1 release closure work: manual Windows media smoke, full regression, packaging, artifact hashes, and release documentation.

**Architecture:** This stage does not add product features. It verifies the existing Tauri + React + Rust + SQLite app on a real Windows desktop, keeps media access restricted to indexed asset IDs, and records release evidence without changing or touching source asset files.

**Tech Stack:** Tauri 2, React 18, TypeScript, Vite, Rust, SQLite/sqlx, WebView2, PowerShell, Vitest, cargo test.

---

## Preconditions

- Current version remains `0.10.1`.
- `I:\GameResManger\docs\superpowers\plans\2026-06-13-v0.10.1-media-preview.md` shows Tasks 103-106 complete.
- `README.md` verification baseline says:
  - `npm test`: 167 frontend tests.
  - `cargo test`: 172 Rust tests plus build script integration.
  - `npm run build`: emits `v0_10_1` assets.
- The executing agent has access to a Windows desktop session with display for Task 107. If no display is available, Task 107 must stop and report "GUI smoke unavailable" without marking itself complete.

## Safety Rules

- Do not delete, move, rename, or modify original asset files.
- Do not write thumbnails, databases, logs, media files, or metadata into source asset folders.
- Do not use batch deletion commands, including `del /s`, `rd /s`, `rmdir /s`, `Remove-Item -Recurse`, or `rm -rf`.
- Do not expand `assetProtocol.scope` beyond:

```json
[
  "$APPDATA/thumbnails/*",
  "$APPDATA/thumbnails/**/*"
]
```

- Do not copy, transcode, or waveform-process media files.
- Do not bump the version unless the user explicitly chooses to create a new release version.
- Do not create a Git commit unless the user explicitly asks.

## File Structure

- Modify: `I:\GameResManger\tests\smoke\README.md`
  - Fill the v0.10.1 native media preview matrix only after actual desktop verification.
- Create: `I:\GameResManger\docs\releases\v0.10.1.md`
  - Record automated checks, GUI smoke status, packaging status, artifact paths, sizes, and SHA-256 hashes.
- Modify: `I:\GameResManger\README.md`
  - Link the new `docs/releases/v0.10.1.md` release record.
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`
  - Update task status and notes as each task completes.

## Task Status Board

| Done | Task | Scope | Required Verification | Notes |
|------|------|-------|-----------------------|-------|
| [ ] | Task 107 | Real Windows GUI smoke for media preview and recent activity | `npm run tauri dev`; filled smoke matrix | Blocked: fixture folder `G:\资源\GameResManagerSmoke\v0.10.1-media` is missing; no interactive GUI control is available in this agent session. See Task 107 Notes below. |
| [x] | Task 108 | Full automated regression and release packaging | metadata tests; `npm test`; `npm run build`; `cargo test`; `cargo check`; `npx tauri build`; `package.ps1` | Completed. `package.ps1` required `pwsh` (PowerShell 7) because Windows PowerShell 5.1 parsed the UTF-8 script as a different encoding. All artifacts recorded with byte sizes and SHA-256 hashes. |
| [x] | Task 109 | Release documentation and README link | Review `docs/releases/v0.10.1.md`; `git diff -- docs/releases/v0.10.1.md README.md tests/smoke/README.md` | Created `docs/releases/v0.10.1.md`; updated README link; GUI smoke explicitly marked unavailable; no source code changed. |
| [x] | Task 110 | Final safety and handoff review | `git status --short`; inspect config scope; no source asset mutation evidence | Completed. Scope remains limited to thumbnails; CSP only adds `asset-media`; `dataDirectory` is `webview-v0.10.1`; working tree contains only docs, smoke notes, and untracked plan files; no source assets modified. No commit made. |

---

### Task 107: Real Windows GUI Smoke For Media Preview

**Files:**
- Modify: `I:\GameResManger\tests\smoke\README.md`
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`

- [ ] **Step 1: Verify clean enough starting state**

Run:

```powershell
cd I:\GameResManger
git status --short
```

Expected:

- Existing unrelated work is not reverted.
- If `docs\superpowers\plans\2026-06-13-v0.10.0-recent-activity-agent-prompts.md` is still untracked, leave it untouched unless the user says otherwise.

- [ ] **Step 2: Prepare fixture media outside the repository**

Create or choose an existing external fixture folder, for example:

```text
G:\资源\GameResManagerSmoke\v0.10.1-media
```

It must contain:

```text
sample.mp3
sample.wav
sample.ogg
sample.mp4
sample.webm
incompatible.mp4
incompatible.webm
```

Expected:

- The folder is outside `I:\GameResManger`.
- The agent does not write cache/database files into this fixture folder.
- If incompatible codec samples are unavailable, record that explicitly in the smoke notes and do not fabricate a pass.

- [ ] **Step 3: Run the desktop app**

Run:

```powershell
cd I:\GameResManger
npm run tauri dev
```

Expected:

- The app window opens.
- No blank screen appears.
- Terminal shows no panic or Tauri command registration error.

- [ ] **Step 4: Index the fixture folder**

In the app:

1. Add the fixture folder as a resource library.
2. Run scan.
3. Wait for scan completion.

Expected:

- All seven fixture files appear in the grid or supported placeholders.
- The source fixture folder still contains the same files at the same paths.

- [ ] **Step 5: Fill the media smoke matrix**

For each supported file, verify:

```text
metadata loads
play starts
pause works
seek works
volume works
switching selection stops old media
first play records one preview_media activity
continuous play does not duplicate the activity
pause and play again records a new activity
file actions remain available
source file remains unchanged
```

For incompatible codec files, verify:

```text
the app attempts native media preview
playback fails into the explicit fallback state
open file / reveal folder / copy path remain available
source file remains unchanged
```

Update the table under `v0.10.1 Native Media Preview Smoke Test` in `tests\smoke\README.md` from `[ ]` to pass/fail notes that reflect real results. Do not mark an untested item as passed.

- [ ] **Step 6: Update Task 107 notes**

Add a note to this plan with:

```text
Fixture folder:
Desktop startup:
Formats passed:
Formats failed:
Recent activity behavior:
Source file safety:
```

Expected:

- Task 107 is marked `[x]` only if the real desktop matrix was run.
- If no desktop display is available, Task 107 stays unchecked and the note says GUI smoke was unavailable.

#### Task 107 Notes (2026-06-17)

Fixture folder:
- Expected path `G:\资源\GameResManagerSmoke\v0.10.1-media` does not exist.
- No alternative fixture folder with the required seven files was found within searchable project-adjacent paths.
- The agent cannot create valid MP3/OGG/MP4/WebM/incompatible codec samples from scratch.

Desktop startup:
- `npm run tauri dev` was executed with a 180-second timeout.
- Vite dev server started on `http://localhost:1420`.
- Rust dev build completed (`game-resource-manager v0.10.1`) with only the pre-existing `list_recent_asset_actions` dead-code warning.
- The debug executable process started and was still running when the timeout terminated it (exit code 143/SIGTERM).
- Window visibility, rendering, and absence of blank screen could not be verified in this non-interactive agent session.

Formats passed:
- None. The interactive matrix was not executed.

Formats failed:
- None. The interactive matrix was not executed.

Recent activity behavior:
- Not verified. No fixture files were indexed and no playback interactions occurred.

Source file safety:
- No source fixture files were accessed, copied, transcoded, deleted, moved, renamed, or modified.
- No cache, database, thumbnail, or log files were written to any fixture folder (no fixture folder existed).
- The working tree contains only documentation updates (`tests/smoke/README.md` and this plan).

Task 107 remains unchecked because the real interactive desktop matrix could not be run.

---

### Task 108: Full Regression And Release Packaging

**Files:**
- May create release artifacts under: `I:\GameResManger\releases`
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`

- [ ] **Step 1: Run metadata tests**

Run:

```powershell
cd I:\GameResManger
npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts
```

Expected:

- All three metadata/cache tests pass.
- Version remains `0.10.1`.
- `dataDirectory` remains `webview-v0.10.1`.

- [ ] **Step 2: Run frontend tests**

Run:

```powershell
cd I:\GameResManger
npm test
```

Expected:

- All frontend tests pass.
- Expected current baseline: 167 tests.

- [ ] **Step 3: Run frontend production build**

Run:

```powershell
cd I:\GameResManger
npm run build
```

Expected:

- Build succeeds.
- Generated JS/CSS names include `v0_10_1`.

- [ ] **Step 4: Run Rust tests and check**

Run:

```powershell
cd I:\GameResManger\src-tauri
cargo test
cargo check
```

Expected:

- Rust tests pass.
- `cargo check` passes.
- A pre-existing `dead_code` warning for `list_recent_asset_actions` is acceptable if still present.

- [ ] **Step 5: Build Tauri release bundle**

Run:

```powershell
cd I:\GameResManger
npx tauri build
```

Expected:

- Tauri release build succeeds.
- A version-matching `0.10.1` installer exists under `src-tauri\target\release\bundle`.

- [ ] **Step 6: Collect release artifacts**

Run:

```powershell
cd I:\GameResManger
.\package.ps1
```

Expected:

- Script creates a timestamped folder under `I:\GameResManger\releases`.
- It copies the `0.10.1` NSIS installer and the portable executable when available.
- It does not recursively delete old output directories.

- [ ] **Step 7: Record artifact inventory**

Run:

```powershell
cd I:\GameResManger
Get-ChildItem -LiteralPath .\releases -Directory | Sort-Object LastWriteTime -Descending | Select-Object -First 1
```

Then run for each artifact in the newest directory:

```powershell
Get-Item -LiteralPath "I:\GameResManger\releases\<latest-folder>\<artifact-name>" | Select-Object Name,Length
Get-FileHash -Algorithm SHA256 -LiteralPath "I:\GameResManger\releases\<latest-folder>\<artifact-name>"
```

Expected:

- Every artifact has a recorded size and SHA-256 hash for Task 109.

#### Task 108 Notes (2026-06-17)

All required commands were run in order on `I:\GameResManger`.

Step 1 - Metadata tests:
- Command: `npm test -- tests/versionConsistency.test.ts tests/viteCacheBust.test.ts tests/tauriWebviewCache.test.ts`
- Result: 3 passed, 3 test files passed.
- Version remains `0.10.1`; `dataDirectory` remains `webview-v0.10.1`.

Step 2 - Frontend tests:
- Command: `npm test`
- Result: 167 passed, 19 test files passed.

Step 3 - Production build:
- Command: `npm run build`
- Result: succeeded.
- Generated assets include `index-v0_10_1-COWjENjP.css` and `index-v0_10_1-DMmitTwd.js`.

Step 4 - Rust tests and check:
- Command: `cd src-tauri && cargo test`
- Result: 149 lib tests + 1 build-script test + 22 media_protocol integration tests = 172 passed.
- Command: `cd src-tauri && cargo check`
- Result: passed with the pre-existing `list_recent_asset_actions` dead-code warning.

Step 5 - Tauri release bundle:
- Command: `npx tauri build`
- Result: succeeded.
- Built `src-tauri\target\release\game-resource-manager.exe`.
- Produced NSIS installer: `src-tauri\target\release\bundle\nsis\游戏资源管理器_0.10.1_x64-setup.exe`.

Step 6 - Packaging script:
- Command: `pwsh -ExecutionPolicy Bypass -File .\package.ps1`
- Note: Windows PowerShell 5.1 parsed the UTF-8 script with the wrong encoding and threw parser errors; PowerShell 7 (`pwsh`) ran the script correctly.
- Result: succeeded.
- Created `I:\GameResManger\releases\v0.10.1-20260617-122418`.
- Did not recursively delete old output directories.

Step 7 - Artifact inventory:
- Latest release directory: `I:\GameResManger\releases\v0.10.1-20260617-122418`

| Artifact | Size (bytes) | SHA-256 |
|---|---:|---|
| `游戏资源管理器_0.10.1_x64-setup.exe` | 5,013,991 | `D1FFDDE4317E2B130A75B56D8E7F70935711DFB85F243727C38DF5DD0E780549` |
| `game-resource-manager.exe` | 20,718,080 | `A173F572BDD3F17C604FA21452AF644DB4C7559B9BA147A9C683612152DDE834` |

Safety:
- No source asset files were accessed or modified.
- No batch deletion commands were used.
- `dist/`, `src-tauri/target/`, and `releases/` were only written to by the normal build/packaging commands.
- Version number was not changed.
- No Git commit was created.

---

### Task 109: Release Documentation And README Link

**Files:**
- Create: `I:\GameResManger\docs\releases\v0.10.1.md`
- Modify: `I:\GameResManger\README.md`
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`

- [ ] **Step 1: Create release record**

Create `docs\releases\v0.10.1.md` with this structure, replacing bracketed values with actual evidence from Tasks 107-108:

```markdown
# v0.10.1 Release Verification

- Verification date: 2026-06-17
- Baseline commit: `[git rev-parse --short HEAD output]`
- Release status: `[automated verification, GUI smoke, packaging status]`

## What's New in v0.10.1

- Native media preview for supported audio (`mp3`, `wav`, `ogg`) and video (`mp4`, `webm`) files in the right inspector.
- Media is served through the restricted `asset-media` protocol by indexed asset ID, not arbitrary absolute path.
- `preview_media` recent activity is recorded only when playback actually starts.
- Codec/load failures show a clear fallback while preserving open, reveal, copy path, notes, tags, and collections.

## Verification

| Check | Result |
|---|---|
| Version and cache metadata tests | `[result]` |
| Frontend tests | `[result]` |
| Frontend production build | `[result]` |
| Rust tests | `[result]` |
| Rust compile check | `[result]` |
| Tauri development startup | `[result]` |
| Native media preview smoke matrix | `[result]` |
| Tauri release bundle | `[result]` |
| Project release packaging | `[result]` |
| Final package interaction smoke | `[result or pending reason]` |

## Release Artifacts

Directory: `[release directory]`

| Artifact | Size | SHA-256 |
|---|---:|---|
| `[artifact]` | `[bytes]` | `[hash]` |

## Desktop Smoke Note

`[summarize fixture folder, formats tested, pass/fail outcomes, recent activity behavior, fallback behavior, and any workstation limitations]`

## Security And Safety

- `assetProtocol.scope` remains limited to thumbnail cache paths.
- Media preview URLs contain only asset IDs.
- No source media file was copied, transcoded, deleted, moved, renamed, or modified.
- Build and release outputs were written only to project build and release directories.
```

Expected:

- No bracketed placeholders remain in the final file.
- If a check could not run, the reason is concrete and dated.

- [ ] **Step 2: Link release record from README**

Modify the README verification section from:

```markdown
The v0.10.0 release verification record lives in [docs/releases/v0.10.0.md](docs/releases/v0.10.0.md).
```

to:

```markdown
The v0.10.1 release verification record lives in [docs/releases/v0.10.1.md](docs/releases/v0.10.1.md).
Earlier release notes are kept in [docs/releases/](docs/releases/).
```

- [ ] **Step 3: Review release documentation diff**

Run:

```powershell
cd I:\GameResManger
git diff -- docs/releases/v0.10.1.md README.md tests/smoke/README.md docs/superpowers/plans/2026-06-17-v0.10.1-release-smoke-and-packaging.md
```

Expected:

- The diff only contains release evidence, smoke results, README link, and this task board update.
- No source code behavior is changed in Task 109.

#### Task 109 Notes (2026-06-17)

Step 1 - Create release record:
- Created `I:\GameResManger\docs\releases\v0.10.1.md`.
- Baseline commit: `cfd03c29`.
- All verification results, artifact sizes, and SHA-256 hashes copied from Task 107-108 Notes.
- Native media preview smoke matrix explicitly marked as `Unavailable` with concrete reasons (missing fixture folder and no interactive GUI control).
- No bracketed placeholders remain.

Step 2 - Link release record from README:
- Updated `README.md` verification section.
- Old text: "The v0.10.0 release verification record lives in [docs/releases/v0.10.0.md](docs/releases/v0.10.0.md)."
- New text: "The v0.10.1 release verification record lives in [docs/releases/v0.10.1.md](docs/releases/v0.10.1.md).\nEarlier release notes are kept in [docs/releases/](docs/releases/)."

Step 3 - Review release documentation diff:
- Ran `git diff -- docs/releases/v0.10.1.md README.md tests/smoke/README.md docs/superpowers/plans/2026-06-17-v0.10.1-release-smoke-and-packaging.md`.
- Confirmed changes are limited to release evidence, smoke notes, README link, and task board/notes updates.
- No source code changes.

---

### Task 110: Final Safety And Handoff Review

**Files:**
- Modify: `I:\GameResManger\docs\superpowers\plans\2026-06-17-v0.10.1-release-smoke-and-packaging.md`

- [ ] **Step 1: Inspect media and asset scope**

Run:

```powershell
cd I:\GameResManger
Select-String -LiteralPath .\src-tauri\tauri.conf.json -Pattern "assetProtocol|scope|media-src|asset-media|webview-v0.10.1" -Context 2,4
```

Expected:

- `assetProtocol.scope` is still limited to thumbnail cache paths.
- CSP includes `asset-media` only for media loading.
- `dataDirectory` is still `webview-v0.10.1`.

- [ ] **Step 2: Inspect working tree**

Run:

```powershell
cd I:\GameResManger
git status --short
```

Expected:

- Changed files are limited to release docs, smoke notes, README, this plan, and generated release artifacts if they are not ignored.
- No source asset paths are staged or changed.
- No commit is created unless the user explicitly requests one.

- [ ] **Step 3: Final handoff note**

Update Task 110 notes with:

```text
Automated verification:
GUI smoke:
Packaging:
Release docs:
Remaining risks:
Git status:
```

Expected:

- The next human or agent can decide whether to commit, tag, upload artifacts, or run additional package interaction smoke.

#### Task 110 Notes (2026-06-17)

Automated verification:
- Metadata/cache tests: 3/3 passed.
- Frontend tests: 167/167 passed.
- Production build: passed (`v0_10_1` versioned assets).
- Rust tests: 172 passed (149 lib + 1 build-script + 22 media_protocol integration).
- Rust compile check: passed with one pre-existing `list_recent_asset_actions` dead-code warning.

GUI smoke:
- `npm run tauri dev` compiled and started the debug executable; window rendering/interaction could not be verified in this non-interactive agent session.
- Native media preview smoke matrix unavailable: fixture folder `G:\资源\GameResManagerSmoke\v0.10.1-media` missing; no interactive GUI control.
- All matrix cells in `tests/smoke/README.md` marked `未测`.

Packaging:
- `npx tauri build` produced the `0.10.1` NSIS installer.
- `package.ps1` (via `pwsh`) collected artifacts to `I:\GameResManger\releases\v0.10.1-20260617-122418`.
- Artifacts: `游戏资源管理器_0.10.1_x64-setup.exe` (5,013,991 bytes) and `game-resource-manager.exe` (20,718,080 bytes); SHA-256 recorded in release docs.

Release docs:
- `docs/releases/v0.10.1.md` created with all verification results, artifact hashes, and safety conclusions.
- `README.md` release record link updated from v0.10.0 to v0.10.1, with earlier release notes directory link.

Remaining risks / pending manual actions:
- Native media preview interactive matrix must be run on a workstation with a display and the required fixture files.
- Final packaged-app interaction smoke (install, launch, basic usage) remains pending.
- Git commit, tag, and artifact upload have not been performed; the user must explicitly request them.

Git status:
```text
 M README.md
 M tests/smoke/README.md
?? docs/releases/v0.10.1.md
?? docs/superpowers/plans/2026-06-13-v0.10.0-recent-activity-agent-prompts.md
?? docs/superpowers/plans/2026-06-17-v0.10.1-release-smoke-and-packaging-agent-prompts.md
?? docs/superpowers/plans/2026-06-17-v0.10.1-release-smoke-and-packaging.md
```
- No source code changes.
- No source asset files modified, moved, renamed, or deleted.
- No batch deletion commands used.
- No commit, tag, PR, or upload action taken.

## Acceptance Criteria

- Real Windows GUI smoke for v0.10.1 media preview is complete or explicitly blocked by missing display/fixtures.
- Automated frontend, backend, metadata, and build checks pass.
- `npx tauri build` and `.\package.ps1` produce version-matching `0.10.1` artifacts.
- `docs/releases/v0.10.1.md` records commands, outcomes, artifact hashes, smoke notes, and safety conclusions.
- `README.md` links to the v0.10.1 release record.
- Original asset files remain untouched.
- No unsafe deletion command is used.
- No version bump or commit is made without explicit user instruction.

## Self-Review

- Spec coverage: this plan covers the known remaining v0.10.1 manual smoke, packaging, release record, and safety verification gaps.
- Placeholder scan: final release output must not contain bracketed placeholder text; Task 109 requires replacing every bracket with real evidence.
- Scope check: no product feature work, no permission expansion, no media copying/transcoding, no source asset mutation.
- Type consistency: no new code types are introduced in this release-closure plan.
