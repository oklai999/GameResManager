# 精准分类与资源查找实施计划

本轮用户已授权拆解并实施。保留上一轮未提交改动与既有文档改动；所有分类数据仅保存在应用数据库。

## 第一阶段：目录范围与类型正确性
- [x] 按索引目录导航，面包屑、直属/递归范围、详情进入同目录、卡片来源提示。
- [x] 搜索独立目录参数，目录边界及参数化匹配；同名排序以 ID 稳定分页。
- [x] JSON/atlas 保守识别 Spine，未识别候选仍保留索引；旧条目重扫更新类型，不破坏标签/收藏。

## 第二阶段：用途标签与筛选
- [x] 标签分组：用途、对象、动作、风格、普通标签；沿用现有标签和批量添加能力。
- [x] ID 精确筛选，同组 OR、跨组 AND、排除标签、尚未分类；条件可单独移除。
- [x] 显示当前结果范围内的标签资源数；分组维护入口。

## 第三阶段：可解释本地规则
- [x] 用户维护目录段/文件名词元规则，绑定现有分类标签；提供示例与匹配原因。
- [x] 当前搜索范围预览、勾选后应用；有明确预览规模限制，不静默截断或后台自动应用。
- [x] 批次记录及撤销；仅撤销该批引入且未被人工接管的关联；人工移除阻止规则重新添加。
- [x] 迁移与 FTS 更新保持事务一致，扫描不触碰人工分类。

## 验收
- [x] 后端：目录边界、直属/递归、分类 OR/AND/排除、计数、类型保留、规则词边界、应用/重复应用/人工接管/移除/撤销、迁移兼容。
- [x] 前端：导航和标签组合、预览错误与过期响应、显式选中应用、批次撤销、构建及既有回归。
- [x] 真实 Tauri 窗口烟测并截图；完成目录收窄、来源提示、分类面板和规则入口检查，未手测项见验证记录。

动态集合、搜索同义词及相关性排名作为后续阶段，不在本轮首批三项内。

阶段验证：npm test 176 项、cargo test 184 项、npm run build、cargo check 均 exit 0。真实桌面范围及限制见 `docs/audits/2026-09-19/classification/verification.md`。
